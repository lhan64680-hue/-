import QtQuick
import QtQuick.Controls
import CineVault

MenuSeparator {
    id: control

    implicitHeight: 9
    padding: 4

    contentItem: Rectangle {
        implicitHeight: 1
        color: Theme.line
    }
}
