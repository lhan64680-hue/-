# 安装包目录

`release.json` 是官网唯一的版本信息来源。首页会优先探测 `serverUrl`：

- 如果服务器上存在对应安装包，则使用官网服务器直连下载。
- 如果安装包不存在或探测失败，则自动回退到 `fallbackUrl`。

正式部署时可把安装包放到本目录：

```text
CineVault-Setup-v0.1.150.exe
```

安装包体积较大，不纳入 Git。更新版本时需要同时修改 `release.json` 的版本号、文件名、体积、SHA-256、发布日期和两个下载地址。
