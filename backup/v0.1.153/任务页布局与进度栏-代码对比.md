# 任务页布局与进度栏 - 代码对比

- 时间：2026-07-03 13:19:37
- 阶段备份：`backup/v0.1.153`
- 当前模块：任务页布局、右侧进度检查栏、视频解析批次进度接口

## 已完成内容

- 任务页左侧 `SourceRail` 已隐藏，不再显示“源素材”栏。
- 任务页内部右侧任务详情栏已移除，任务列表区域改为占满主体区域。
- 最右侧普通素材检查器在任务页改为 `JobProgressInspectorPane`，显示当前处理素材进度和批次统计。
- “同步全局索引”仍保留在任务页列表与底部 `JobTimelineBar` 中，不再作为任务页内部右侧独立详情栏显示。
- 已补齐 `G:\data\app\DIT\ffmpeg-dev` FFmpeg 开发包，并完成 Release + FFmpeg 安装包构建。

## 具体修改的代码前后对比

### 1. 任务页隐藏源素材栏，并切换右侧检查器

修改前：

```qml
SourceRail {
    Layout.preferredWidth: root.shellViewModel.currentWorkspace === root.shellViewModel.materialCenterWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialBackupWorkspaceId
        || root.isFeedbackWorkspace
        || root.isProjectLibraryWorkspace
        || !root.hasOpenProject
        ? 0
        : (root.sourceRailCollapsed ? 56 : 270)
    visible: root.shellViewModel.currentWorkspace !== root.shellViewModel.materialCenterWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialBackupWorkspaceId
        && !root.isFeedbackWorkspace
        && !root.isProjectLibraryWorkspace
        && root.hasOpenProject
}

InspectorPane {
    Layout.preferredWidth: root.shellViewModel.currentWorkspace === root.shellViewModel.reportWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialCenterWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialBackupWorkspaceId
        || root.isFeedbackWorkspace
        || root.isProjectLibraryWorkspace
        || !root.hasOpenProject
        ? 0
        : 330
}
```

修改后：

```qml
readonly property bool isJobsWorkspace: root.shellViewModel && root.shellViewModel.currentWorkspace === root.shellViewModel.jobsWorkspaceId

SourceRail {
    Layout.preferredWidth: root.shellViewModel.currentWorkspace === root.shellViewModel.materialCenterWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialBackupWorkspaceId
        || root.isJobsWorkspace
        || root.isFeedbackWorkspace
        || root.isProjectLibraryWorkspace
        || !root.hasOpenProject
        ? 0
        : (root.sourceRailCollapsed ? 56 : 270)
    visible: root.shellViewModel.currentWorkspace !== root.shellViewModel.materialCenterWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialBackupWorkspaceId
        && !root.isJobsWorkspace
        && !root.isFeedbackWorkspace
        && !root.isProjectLibraryWorkspace
        && root.hasOpenProject
}

InspectorPane {
    Layout.preferredWidth: root.shellViewModel.currentWorkspace === root.shellViewModel.reportWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialCenterWorkspaceId
        || root.shellViewModel.currentWorkspace === root.shellViewModel.materialBackupWorkspaceId
        || root.isJobsWorkspace
        || root.isFeedbackWorkspace
        || root.isProjectLibraryWorkspace
        || !root.hasOpenProject
        ? 0
        : 330
    visible: root.shellViewModel.currentWorkspace !== root.shellViewModel.reportWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialCenterWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialBackupWorkspaceId
        && !root.isJobsWorkspace
        && !root.isFeedbackWorkspace
        && !root.isProjectLibraryWorkspace
        && root.hasOpenProject
}

JobProgressInspectorPane {
    Layout.preferredWidth: root.isJobsWorkspace && root.hasOpenProject ? 330 : 0
    Layout.fillHeight: true
    visible: root.isJobsWorkspace && root.hasOpenProject
    viewModel: root.jobTimelineViewModel
}
```

### 2. 任务页内部右侧详情栏移除

修改前：

```qml
RowLayout {
    Layout.fillWidth: true
    Layout.fillHeight: true
    spacing: 14

    ColumnLayout {
        Layout.fillWidth: true
        Layout.fillHeight: true
        Layout.preferredWidth: 780
        spacing: 14
        // 总量进度卡 + 任务列表
    }

    Rectangle {
        Layout.preferredWidth: 420
        Layout.fillHeight: true
        // 选择左侧任务查看详情 / 当前状态反馈 / 解析批次补充信息
    }
}
```

修改后：

```qml
ColumnLayout {
    Layout.fillWidth: true
    Layout.fillHeight: true
    spacing: 14

    ColumnLayout {
        Layout.fillWidth: true
        Layout.fillHeight: true
        spacing: 14
        // 总量进度卡 + 任务列表
    }
}
```

### 3. 视频解析服务公开当前素材进度

修改前：

```cpp
qint64 batchProgressPercent() const;
QString batchCurrentLabel() const;
QString batchStatusText() const;
```

修改后：

```cpp
qint64 batchProgressPercent() const;
qint64 batchCurrentProgressPercent() const;
QString batchCurrentLabel() const;
QString batchCurrentDetail() const;
QString batchStatusText() const;
```

新增实现：

```cpp
qint64 VideoAnalysisService::batchCurrentProgressPercent() const
{
    if (m_currentVideoKey.trimmed().isEmpty()) {
        return 0;
    }
    return qBound<qint64>(0LL, m_batchCurrentProgress, 100LL);
}

QString VideoAnalysisService::batchCurrentDetail() const
{
    if (m_currentVideoKey.trimmed().isEmpty()) {
        return QStringLiteral("当前没有正在处理的素材。");
    }

    const auto detail = m_batchCurrentDetail.trimmed();
    return detail.isEmpty() ? QStringLiteral("正在处理当前素材。") : detail;
}
```

### 4. 任务 ViewModel 暴露给 QML

修改前：

```cpp
Q_PROPERTY(QString batchCurrentLabel READ batchCurrentLabel NOTIFY stateChanged)
```

修改后：

```cpp
Q_PROPERTY(QString batchCurrentLabel READ batchCurrentLabel NOTIFY stateChanged)
Q_PROPERTY(QString batchCurrentDetail READ batchCurrentDetail NOTIFY stateChanged)
Q_PROPERTY(int batchCurrentProgress READ batchCurrentProgress NOTIFY stateChanged)
Q_PROPERTY(bool hasActiveBatchItem READ hasActiveBatchItem NOTIFY stateChanged)
```

新增实现：

```cpp
QString JobTimelineViewModel::batchCurrentDetail() const
{
    return m_videoAnalysisService
        ? m_videoAnalysisService->batchCurrentDetail()
        : QStringLiteral("当前没有正在处理的素材。");
}

int JobTimelineViewModel::batchCurrentProgress() const
{
    return m_videoAnalysisService ? static_cast<int>(m_videoAnalysisService->batchCurrentProgressPercent()) : 0;
}

bool JobTimelineViewModel::hasActiveBatchItem() const
{
    return m_videoAnalysisService && !m_videoAnalysisService->batchCurrentLabel().trimmed().isEmpty();
}
```

### 5. 新增右侧进度检查栏组件

新增文件：`src/ui/qml/components/JobProgressInspectorPane.qml`

```qml
Rectangle {
    property var viewModel
    readonly property bool hasCurrentItem: viewModel && viewModel.hasActiveBatchItem
    readonly property int currentProgress: viewModel ? viewModel.batchCurrentProgress : 0
    readonly property int batchProgress: viewModel ? viewModel.batchProgress : 0

    // 显示：当前素材、当前素材进度、批次总进度、已处理/成功/失败/排队、当前状态反馈
}
```

`CMakeLists.txt` 同步登记：

```cmake
src/ui/qml/components/JobProgressInspectorPane.qml
```

## 验证结果

- `git diff --check`：通过，仅有既有换行符提示。
- `powershell -ExecutionPolicy Bypass -File .\tool\build_windows.ps1 -Configuration Release -EnableFfmpeg`：通过。
- 已生成安装包：`output/v0.1.124/CineVault-Setup-v0.1.124.exe`。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-ffmpeg --output-on-failure -C Release`：8/8 通过。
- 已清理临时下载缓存：`%TEMP%\cinevault-ffmpeg-dev`。
- 已清理安装 staging：`output/_installer_staging`。

## 待办清单

- 建议打开安装版进入“任务”页，实机确认左侧源素材栏隐藏、右侧进度检查栏显示、底部总任务栏仍显示同步索引任务。

## 下一步

- 等待实机 UI 确认；若还需要右侧栏自动显示当前处理素材的视频画面，可在已有进度栏基础上继续接入视频 key 到素材 URL 的查询链路。
