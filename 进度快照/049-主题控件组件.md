# 进度快照 049

- 时间：2026-06-26 12:42:22
- 已完成内容：新增主题化通用控件 `ThemedTextField`、`ThemedComboBox`、`ThemedSpinBox`、`ThemedProgressBar`、`ThemedScrollBar`、`ThemedMenu`、`ThemedMenuItem`、`ThemedMenuSeparator`；`ActionButton` 改为使用 `Theme` 的按钮语义色；新增控件已注册到 `CMakeLists.txt` 的 QML 模块。
- 当前修改模块：通用主题控件模块，涉及 `src/ui/qml/components/Themed*.qml`、`ActionButton.qml`、`CMakeLists.txt`。
- 代码前后对比：
  - 修改前：页面直接使用 Qt 默认 `TextField`、`ComboBox`、`SpinBox`、`ProgressBar`、`MenuItem` 时容易出现系统白底控件。
  - 修改后：新增主题控件统一设置背景、文字、边框、悬停、按下、禁用、下拉弹层和滚动条颜色。
  - 修改前：`ActionButton` 的普通/主按钮悬停和按下色使用硬编码深色。
  - 修改后：`ActionButton` 使用 `Theme.button*` 和 `Theme.primary*` 语义色，后续可随明暗主题动态切换。
- 待办清单（未完成）：将素材管理中心、设置页、素材库、报表页、顶部栏、任务栏接入主题控件；补充浅色模式下的硬编码背景替换；构建安装包并烟测。
- 下一步要做什么：进入页面接入模块，优先修复素材管理中心顶部搜索框、筛选框和下拉列表，再扩展到设置页与其他主要页面。
