# 097-素材库拖拽浏览优化

日期：2026-06-27

## 已完成内容

- 已读取上一轮最新快照：`096-素材库图片文档空格全屏预览.md`。
- 已在本模块开始前执行源码备份，生成 `backup/v0.1.81`。
- 已完成模块二：素材库内容区拖拽浏览优化。
- 已为素材库网格模式补齐可拖动纵向浏览条。
- 已为技术表格模式补齐固定在视口右侧的纵向浏览条，并保留底部横向浏览条。
- 已新增左键拖拽内容区浏览能力，支持网格与表格两种模式。
- 已启用 `reuseItems` 与 `cacheBuffer`，优化巨量素材浏览时的视图复用效率。
- 已新增模块溯源文档：`backup/v0.1.81/素材库拖拽浏览优化-前后对比.md`。

## 当前修改到哪个模块

当前模块：模块二，素材库内容区拖拽浏览优化。

修改文件：

- `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/LibraryWorkspace.qml`
- `dit-tools-src/cinevault-pro/src/ui/qml/components/ThemedScrollBar.qml`

## 具体修改的代码前后对比

### 1. 网格模式增加浏览条与拖拽浏览

修改前：

```qml
GridView {
    clip: true
    cellWidth: libraryRoot.gridCellWidth
    cellHeight: libraryRoot.gridCellHeight
    model: viewModel.model
    currentIndex: viewModel ? viewModel.selectedAssetIndex : -1
}
```

修改后：

```qml
GridView {
    id: assetGrid
    clip: true
    cellWidth: libraryRoot.gridCellWidth
    cellHeight: libraryRoot.gridCellHeight
    boundsBehavior: Flickable.StopAtBounds
    reuseItems: true
    cacheBuffer: libraryRoot.gridCellHeight * 6
    model: viewModel.model
    currentIndex: viewModel ? viewModel.selectedAssetIndex : -1

    ScrollBar.vertical: ThemedScrollBar {
        policy: ScrollBar.AsNeeded
    }

    DragHandler {
        target: null
        acceptedDevices: PointerDevice.Mouse
        acceptedButtons: Qt.LeftButton
        grabPermissions: PointerHandler.CanTakeOverFromAnything
    }
}
```

### 2. 表格模式增加固定纵向浏览条与双向拖拽

修改前：

```qml
Flickable {
    id: tableFlick
    clip: true
    contentWidth: Math.max(width, 1160)
    contentHeight: height
    boundsBehavior: Flickable.StopAtBounds
    ScrollBar.horizontal: ThemedScrollBar { policy: ScrollBar.AsNeeded }
}
```

修改后：

```qml
Flickable {
    id: tableFlick
    clip: true
    contentWidth: Math.max(width, 1160)
    contentHeight: height
    boundsBehavior: Flickable.StopAtBounds
    property real dragLastX: 0
    property real dragLastY: 0
    ScrollBar.horizontal: ThemedScrollBar { policy: ScrollBar.AsNeeded }

    DragHandler {
        target: null
        acceptedDevices: PointerDevice.Mouse
        acceptedButtons: Qt.LeftButton
        grabPermissions: PointerHandler.CanTakeOverFromAnything
    }

    ListView {
        id: tableList
        reuseItems: true
        cacheBuffer: 720
        ScrollBar.vertical: ThemedScrollBar {
            parent: tableFlick.parent
            anchors.top: tableFlick.top
            anchors.right: tableFlick.right
            anchors.bottom: tableFlick.bottom
            policy: ScrollBar.AsNeeded
        }
    }
}
```

### 3. 滚动条主题样式增强可见性

修改前：

```qml
ScrollBar {
    policy: ScrollBar.AsNeeded
    minimumSize: 0.08
    padding: 2

    background: Rectangle {
        color: "transparent"
    }
}
```

修改后：

```qml
ScrollBar {
    policy: ScrollBar.AsNeeded
    minimumSize: 0.08
    padding: 3

    background: Rectangle {
        implicitWidth: control.orientation === Qt.Vertical ? 12 : 100
        implicitHeight: control.orientation === Qt.Vertical ? 100 : 12
        radius: control.orientation === Qt.Vertical ? width / 2 : height / 2
        color: control.size < 1.0
               ? Qt.rgba(Theme.line.r, Theme.line.g, Theme.line.b, control.hovered || control.pressed ? 0.34 : 0.18)
               : "transparent"
    }
}
```

## 验证结果

- 直接在未注入 MSVC 环境的终端执行 `cmake --build` 时，因缺少标准库头文件路径报错；已改用项目自带 `windows_toolchain.ps1` 包装命令完成验证。
- `cmake --build --preset windows-msvc-release --config Release` 通过。
- `ctest --test-dir build/windows-msvc-release --output-on-failure`：3/3 通过。
- `cmake --build --preset windows-msvc-release-real --config Release` 通过。
- `ctest --test-dir build/windows-msvc-release-real --output-on-failure`：3/3 通过。

## 待办清单（未完成）

- 模块三：素材管理中心已解析素材切换卡顿优化到丝滑体验。
- 人工复核图片、PDF、Markdown、CSV、DOCX、XLSX、PPTX 样本的全屏预览实际效果。
- 人工复核旧版 `doc/xls/ppt` 的降级提示文案是否符合预期。
- 人工复核素材库左键拖拽浏览在超大列表下的手感与误触情况。

## 下一步要做什么

进入模块三，优化素材管理中心已解析素材切换时的卡顿问题，然后继续做一次全局构建验证。
