# 100-素材管理中心逐帧渐进加载

日期：2026-06-27

## 已完成内容

- 已读取上一轮最新快照：`099-素材管理中心逐帧列表分段渲染.md`。
- 已在本模块开始前执行源码备份，生成 `backup/v0.1.84`。
- 已完成新模块：素材管理中心逐帧结果渐进加载。
- 已把逐帧区从“首批 24 帧 / 一次性展开全部”升级为“首批 24 帧 / 再加载一批 / 全部展开 / 收起”。
- 已新增剩余帧数和是否可继续加载等前端属性。
- 已将逐帧区内部状态从“布尔展开”调整为“可见帧上限”，让分批加载和收起逻辑更稳定。
- 已新增“当前显示 x / y 帧，剩余 z 帧”状态文案。
- 已保持搜索命中、逐帧图片点击查看和现有详情结构不变。
- 已新增模块溯源文档：`backup/v0.1.84/素材管理中心逐帧渐进加载-前后对比.md`。

## 当前修改到哪个模块

当前模块：素材管理中心逐帧结果渐进加载。

修改文件：

- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.h`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.cpp`
- `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialCenterWorkspace.qml`

## 具体修改的代码前后对比

### 1. 新增剩余数量与继续加载能力

修改前：

```cpp
Q_PROPERTY(int selectedFrameCount READ selectedFrameCount NOTIFY selectionChanged)
Q_PROPERTY(int selectedVisibleFrameCount READ selectedVisibleFrameCount NOTIFY selectionChanged)
Q_PROPERTY(bool selectedFramesExpanded READ selectedFramesExpanded NOTIFY selectionChanged)
Q_PROPERTY(bool canExpandSelectedFrames READ canExpandSelectedFrames NOTIFY selectionChanged)
```

修改后：

```cpp
Q_PROPERTY(int selectedFrameCount READ selectedFrameCount NOTIFY selectionChanged)
Q_PROPERTY(int selectedVisibleFrameCount READ selectedVisibleFrameCount NOTIFY selectionChanged)
Q_PROPERTY(int selectedRemainingFrameCount READ selectedRemainingFrameCount NOTIFY selectionChanged)
Q_PROPERTY(bool selectedFramesExpanded READ selectedFramesExpanded NOTIFY selectionChanged)
Q_PROPERTY(bool canExpandSelectedFrames READ canExpandSelectedFrames NOTIFY selectionChanged)
Q_PROPERTY(bool canLoadMoreSelectedFrames READ canLoadMoreSelectedFrames NOTIFY selectionChanged)
```

### 2. 逐帧区改为可见帧上限控制

修改前：

```cpp
bool m_selectedFramesExpanded = false;
```

修改后：

```cpp
int m_selectedVisibleFrameLimit = 24;
```

并新增：

```cpp
void MaterialCenterViewModel::loadMoreSelectedFrames()
{
    m_selectedVisibleFrameLimit += kVisibleFrameBatchSize;
    applySelectedFrameExpansion();
    emit selectionChanged();
}

void MaterialCenterViewModel::showAllSelectedFrames()
{
    m_selectedVisibleFrameLimit = selectedFrameCount();
    applySelectedFrameExpansion();
    emit selectionChanged();
}

void MaterialCenterViewModel::collapseSelectedFrames()
{
    m_selectedVisibleFrameLimit = kInitialVisibleFrameCount;
    applySelectedFrameExpansion();
    emit selectionChanged();
}
```

### 3. 右侧逐帧区新增“再加载一批 / 全部展开 / 收起”

修改前：

```qml
ActionButton {
    Layout.preferredWidth: 96
    Layout.preferredHeight: 30
    visible: viewModel && viewModel.canExpandSelectedFrames
    text: viewModel && viewModel.selectedFramesExpanded ? "收起" : "展开全部"
    onClicked: if (viewModel) viewModel.toggleSelectedFramesExpanded()
}
```

修改后：

```qml
ActionButton {
    Layout.preferredWidth: 106
    Layout.preferredHeight: 30
    visible: viewModel && viewModel.canLoadMoreSelectedFrames
    text: "再加载一批"
    onClicked: if (viewModel) viewModel.loadMoreSelectedFrames()
}

ActionButton {
    Layout.preferredWidth: 96
    Layout.preferredHeight: 30
    visible: viewModel && viewModel.canLoadMoreSelectedFrames
    text: "全部展开"
    onClicked: if (viewModel) viewModel.showAllSelectedFrames()
}

ActionButton {
    Layout.preferredWidth: 84
    Layout.preferredHeight: 30
    visible: viewModel && viewModel.canExpandSelectedFrames && viewModel.selectedFramesExpanded
    text: "收起"
    onClicked: if (viewModel) viewModel.collapseSelectedFrames()
}
```

## 验证结果

- `cmake --build --preset windows-msvc-release --config Release` 通过。
- `ctest --test-dir build/windows-msvc-release --output-on-failure`：3/3 通过。
- `cmake --build --preset windows-msvc-release-real --config Release` 通过。
- `ctest --test-dir build/windows-msvc-release-real --output-on-failure`：3/3 通过。

## 待办清单（未完成）

- 人工复核素材管理中心在真实已解析大项目中连续上下切换素材时的体感是否达到预期。
- 人工复核“再加载一批 / 全部展开 / 收起”在超大逐帧结果下的滚动手感和恢复速度。
- 人工复核素材管理中心首次切到某个尚未生成多宫格拼图的视频时，是否会先显示原缩略图再平滑切到多宫格拼图。
- 人工复核图片、PDF、Markdown、CSV、DOCX、XLSX、PPTX 样本的全屏预览实际效果。
- 人工复核旧版 `doc/xls/ppt` 的降级提示文案是否符合预期。
- 人工复核素材库左键拖拽浏览在超大列表下的手感与误触情况。

## 下一步要做什么

如果继续自动推进，我会优先继续把素材管理中心详情区往真正的虚拟化列表方向收敛，或者开始做真实项目体验手测前的辅助状态显示与诊断入口。
