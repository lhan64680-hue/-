# 141-开发者Web对话台

日期：2026-07-01

## 已完成内容

- 已完成模块 6：开发者 Web 对话台。
- 已补全 `services/feedback/admin`：
  - `index.html`
  - `styles.css`
  - `app.js`
- 已完成聊天软件式双栏布局：
  - 左侧会话列表
  - 中间消息流与回复区
  - 右侧用户资料与指标区
- 已完成管理员登录、token 持久化、会话搜索、状态切换、附件发送。
- 已完成 WebSocket 实时收消息与断线自动重连。
- 已完成管理端与模块 5 后端协议对接。

## 当前修改到哪个模块

当前完成模块：006 开发者 Web 对话台。

## 具体修改的代码前后对比

### 修改前

```html
<div id="conversation-list" class="conversation-list"></div>
<div id="message-list" class="message-list">
  <div class="empty-state">选择会话后，这里会显示完整聊天记录。</div>
</div>
```

### 修改后

```js
async function loadConversations() {
  const data = await requestJson(endpoint("api/admin/conversations"), {
    headers: authHeaders(),
  });
  state.conversations = data.conversations || [];
  renderConversationList();
}
```

```js
function connectSocket() {
  const socket = new WebSocket(`${wsEndpoint("ws/admin")}?token=${encodeURIComponent(state.token)}`);
  state.socket = socket;
}
```

```css
.chat-card {
  display: grid;
  grid-template-rows: minmax(0, 1fr) auto;
  overflow: hidden;
}
```

## 验证结果

- 管理端静态资源存在并可由后端返回：通过
- 管理端接口对接 smoke：通过
- 页面级浏览器手工视觉验收：待和桌面端一起做联调烟测

## 待办清单（未完成）

- 模块 7：桌面端“使用反馈”页面与实时提醒
- 模块 8：联调、构建、部署、清理

## 下一步要做什么

下一步进入桌面端反馈页，新增 `WorkspaceId::Feedback`、反馈服务、消息 list model、反馈 view model 和 QML 工作区页面，并把未读提醒挂到顶栏标签上。
