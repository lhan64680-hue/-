# 126-修复素材管理中心迁移顺序

日期：2026-06-30

## 已完成内容

- 已读取上一轮最新快照：`125-github发布v0.1.95.md`。
- 已创建新的阶段备份：`backup/v0.1.107`。
- 已定位素材管理中心无法加载素材的真实根因：
  - 不是素材丢失
  - 是老库迁移顺序错误导致全局库打开失败
- 已确认真实安装目录：
  - `D:\Program Files\影资管家`
- 已确认真实数据库：
  - `D:\Program Files\影资管家\data\material-center.sqlite`
- 已确认真实库现状：
  - `global_video_asset` 仍有 `148` 条素材
  - `video_frame_analysis` 缺少 `analysis_state` 等新增字段
- 已修复 `GlobalDatabaseManager::ensureSchemaCompatibility()`：
  - 先创建任务表
  - 再补 `video_frame_analysis` 字段
  - 再补 `video_analysis_task` 字段
  - 最后才创建 `idx_frame_video_state` 索引
- 已用真实库副本验证修复后的迁移顺序可以成功补字段并恢复逐帧查询。
- 已完成构建验证：
  - `cmake --build G:/data/app/DIT-tools/dit-tools-src/cinevault-pro/build/windows-msvc-release-real --config Release`
- 已完成测试验证：
  - `ctest --test-dir G:/data/app/DIT-tools/dit-tools-src/cinevault-pro/build/windows-msvc-release-real -C Release --output-on-failure`
  - `6/6` 通过

## 当前修改到哪个模块

当前完成模块：素材管理中心数据库迁移修复模块。

这次回归问题已经从“页面空白表现”追到具体数据库迁移错误，修复点集中在一个文件：

- `src/infrastructure/db/GlobalDatabaseManager.cpp`

## 具体修改的代码前后对比

### 修改前

```text
- ensureSchemaCompatibility() 先创建依赖 analysis_state 的索引。
- 老库缺 analysis_state 时，CREATE INDEX 直接失败。
- 后续 ALTER TABLE 不会执行。
- openDatabase() 返回失败，素材管理中心查询层整体回空。
```

### 修改后

```text
- ensureSchemaCompatibility() 先补字段，再建索引。
- 同时给 video_analysis_task 也补上兼容字段检查。
- 老库可以完成迁移并恢复素材加载。
```

## 验证结果

- 真实安装库素材数核对：`148`
- 真实库副本迁移后逐帧查询：通过
- `cmake --build ... --config Release`：通过
- `ctest ... -C Release --output-on-failure`：通过

## 待办清单（未完成）

- 发布修复版安装包
- 推送 GitHub 更新
- 让你用新包复测素材管理中心加载与解析续跑主路径

## 下一步要做什么

继续做发布模块，生成一个新的修复版本安装包并推送到 GitHub，供你直接更新测试。
