# 进度快照 085

- 时间：2026-06-27
- 已完成内容：素材备份页面接入真实源/目的地/任务列表、预检、启动取消和进度展示。
- 当前修改模块：模块 5，页面交互完整化。
- 阶段备份：
  - `backup/v0.1.70`
- 修改文件：
  - `dit-tools-src/cinevault-pro/CMakeLists.txt`
  - `dit-tools-src/cinevault-pro/src/app/AppContext.h`
  - `dit-tools-src/cinevault-pro/src/app/AppContext.cpp`
  - `dit-tools-src/cinevault-pro/src/application/MaterialBackupService.h`
  - `dit-tools-src/cinevault-pro/src/application/MaterialBackupService.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupSourceListModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupSourceListModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupDestinationListModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupDestinationListModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupTaskListModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/models/BackupTaskListModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialBackupWorkspace.qml`
  - `docs/素材备份功能开发方案.md`

## 已完成内容

- 新增 `BackupSourceListModel`、`BackupDestinationListModel`、`BackupTaskListModel`。
- 新增 `MaterialBackupService`，负责构建计划、启动异步备份、转发任务进度、更新 `JobEngine` 和发出完成结果。
- `MaterialBackupViewModel` 接入添加文件、添加文件夹、添加磁盘卷、添加目的地、移除、设置主目标、校验模式、级联开关、开始和取消。
- `MaterialBackupWorkspace.qml` 从静态骨架升级为真实三栏交互页面。
- Minimal ViewModel 补齐同名属性和空操作方法，保证最小 GUI 构建稳定。
- 页面右侧显示总体进度、每目的地进度、速度、状态和错误摘要。

## 当前修改到哪个模块

- 当前完成模块：模块 5，页面交互完整化。
- 下一模块：模块 6，素材库归档联动。

## 代码前后对比

### 1. 页面按钮

修改前：

```qml
ActionButton {
    text: "添加文件"
    enabled: false
}
```

修改后：

```qml
ActionButton {
    text: "添加文件"
    enabled: viewModel && !viewModel.running
    onClicked: viewModel.addFileSources()
}
```

### 2. ViewModel 启动备份

修改前：

```cpp
class MaterialBackupViewModel : public QObject {
    Q_PROPERTY(QString summaryText READ summaryText NOTIFY stateChanged)
};
```

修改后：

```cpp
Q_INVOKABLE void addFileSources();
Q_INVOKABLE void addFolderSource();
Q_INVOKABLE void addVolumeSource();
Q_INVOKABLE void addDestination();
Q_INVOKABLE void startBackup();
Q_INVOKABLE void cancelBackup();
```

### 3. 服务层

修改前：

```text
src/application/MaterialBackupService.* 不存在。
```

修改后：

```cpp
class MaterialBackupService : public QObject {
public:
    BackupPlan buildPlan(const BackupRequest &request) const;
public slots:
    bool startBackup(const BackupPlan &plan);
    void cancelBackup();
signals:
    void backupTaskUpdated(const BackupDestinationTask &task);
    void backupFinished(const BackupExecutionResult &result);
};
```

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.70`。
- 最小 GUI 构建：`cmake --build --preset windows-msvc-release --config Release`，成功。
- 真实工作流构建：`cmake --build --preset windows-msvc-release-real --config Release`，成功。
- 最小 GUI 测试：`ctest --test-dir build/windows-msvc-release --output-on-failure`，2/2 通过。
- 真实工作流测试：`ctest --test-dir build/windows-msvc-release-real --output-on-failure`，2/2 通过。
- 修复记录：真实工作流首次编译 `MaterialBackupService.cpp` 时 `qBound` 参数类型不一致，已改为显式 `qint64`。

## 待办清单（未完成）

- 模块 6：单个成功路径自动导入素材库。
- 模块 6：多个成功路径弹窗选择归档路径再导入素材库。
- 模块 7：最终构建、测试、打包和清理。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材备份功能开发方案.md`。下一步从模块 6 开始，先执行 `bash tool/create_backup.sh` 创建下一阶段备份，再让 `MaterialBackupViewModel` 在 `backupFinished` 后调用现有 `ImportService::importDirectory` 完成素材库归档。
