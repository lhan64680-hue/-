# 进度快照 061

- 时间：2026-06-26 17:32:00 +08:00
- 已完成内容：完成项目生成产物归档路径调整，新生成缩略图、报表预览图、视频解析帧会写入对应项目文件夹。
- 当前修改模块：项目文件夹产物归档模块。
- 代码前后对比：
  - 修改前：缩略图写入应用级 `cache/thumbnails`，报表预览图写入应用级 `cache/report-preview`，视频解析帧写入应用级 `data/frame-cache`。
  - 修改后：新增项目级路径工具，缩略图写入 `<项目根>/cache/thumbnails`，报表预览写入 `<项目根>/cache/report-preview`，视频解析帧写入 `<项目根>/analysis/frames`。
  - 修改前：`MediaTaskService` 只按素材源和素材 ID 生成应用级缩略图路径。
  - 修改后：`MediaTaskService` 捕获当前项目数据库路径，并通过项目根目录生成缩略图路径。
  - 修改前：素材管理中心选中视频时，解析图片目录指向旧的全局帧缓存位置。
  - 修改后：选中视频的解析图片目录根据该视频所属项目数据库路径计算，打开的是项目级解析帧目录。
- 验证结果：
  - 已运行局部 `git diff --check`，本模块修改未发现空白错误；仅输出既有 LF/CRLF 提示。
  - 已搜索旧产物路径调用，除保留兼容用的 `Paths::frameCacheDirectory` 定义外，新生成产物路径已切换到项目级接口。
- 待办清单（未完成）：
  - 模块 3：补齐项目库右键菜单和卡片按钮。
  - 模块 4：构建安装包、运行 `ctest`、启动烟测、清理临时缓存。
- 下一步要做什么：继续修改项目库 ViewModel 与 QML，让卡片和右键菜单都能触发重命名、移动位置、删除。
