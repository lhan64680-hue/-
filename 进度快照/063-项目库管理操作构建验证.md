# 进度快照 063

- 时间：2026-06-26 18:02:00 +08:00
- 已完成内容：完成项目库管理操作、项目文件夹产物归档、构建验证、烟测和临时缓存清理。
- 当前修改模块：项目库管理操作整体验证模块。
- 阶段备份：已创建 `backup/v0.1.49`，并新增 `backup/v0.1.49/项目库管理操作-前后对比.md` 记录本阶段关键代码前后对比和验证结果。
- 代码前后对比：
  - 修改前：项目库只能打开项目或从列表移除入口，不能真实重命名、移动或删除项目文件夹。
  - 修改后：项目库服务层支持重命名、移动位置、移入系统回收站删除，并同步项目数据库、最近/长期项目索引和素材管理中心全局索引。
  - 修改前：项目卡片没有操作按钮，右键菜单缺少重命名、移动、删除。
  - 修改后：项目卡片和右键菜单均提供打开、重命名、移动位置、删除/移除缺失入口。
  - 修改前：缩略图、报表预览、视频解析帧写入应用级缓存目录。
  - 修改后：新生成产物写入对应项目文件夹下的 `cache/thumbnails`、`cache/report-preview`、`analysis/frames`。
  - 修改前：项目目录只确保 `exports`、`logs`、`reports`。
  - 修改后：新建/打开项目会确保完整项目数据目录结构。
- 验证结果：
  - 全局空白检查：`git diff --check` 成功退出，仅有既有 LF/CRLF 提示。
  - 构建命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`
  - 最终产物：`output/v0.1.58/CineVault-Setup-v0.1.58.exe`
  - 安装包大小：`55282417`
  - SHA256：`DD276D79F3367DBE2B53DC08A3E0CDA8081E17EA64258CF523C7C43161B1C6E6`
  - `ctest`：命令成功退出，当前构建目录输出 `No tests were found!!!`
  - 5 秒启动烟测：通过，状态 `SMOKE_RUNNING_AFTER_5S`，`NO_QML_LOG`
  - 临时缓存：`output/_smoke_runtime` 未残留；空的 `output/_installer_staging` 已删除。
- 待办清单（未完成）：
  - 安装或运行 `output/v0.1.58/CineVault-Setup-v0.1.58.exe` 后人工复核真实 UI：项目重命名、移动位置、删除到回收站、缺失项目移除入口。
  - 使用真实素材项目复核新生成缩略图、报表预览图、视频解析帧是否落在项目文件夹内。
- 下一步要做什么：人工复核项目库操作；如需要迁移历史缓存到项目文件夹，再开启单独阶段处理。
