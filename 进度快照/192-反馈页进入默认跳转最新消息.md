# 进度快照 192

- 时间：2026-07-03 10:51:30
- 当前模块：使用反馈页面消息列表滚动

## 已完成内容

- 已读取最新快照：`进度快照/191-v0.1.122已发布到GitHub.md`。
- 已完成阶段备份：`backup/v0.1.150`。
- 已新增本阶段代码对比文档：`backup/v0.1.150/反馈页进入默认跳转最新消息-代码对比.md`。
- 已修复“每次进入 使用反馈 页面时，信息窗口显示最早信息位置”的问题：
  - `Main.qml` 向 `FeedbackWorkspace` 传入真实的反馈页激活状态 `workspaceActive`。
  - `FeedbackWorkspace.qml` 不再依赖缓存 Loader 子项自身 `visible` 判断页面进入状态。
  - 聊天消息列表在组件首次加载、页面切回反馈页、消息从空到有时都会调度 `positionViewAtEnd()`。
  - 保留原有发送成功后跳转最新消息逻辑。
- 已更新构建产物：
  - `output/v0.1.123/CineVault-Setup-v0.1.123.exe`
- 已清理构建临时目录：
  - `output/_installer_staging`

## 当前修改到哪个模块

- 桌面端 QML：使用反馈页面。
- 具体文件：
  - `dit-tools-src/cinevault-pro/src/ui/qml/Main.qml`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/FeedbackWorkspace.qml`

## 具体修改的代码前后对比

### 1. `Main.qml` 传入真实页面激活状态

修改前：

```qml
FeedbackWorkspace {
    viewModel: root.feedbackViewModel
    documentPreviewVm: root.documentPreviewViewModel
}
```

修改后：

```qml
FeedbackWorkspace {
    viewModel: root.feedbackViewModel
    documentPreviewVm: root.documentPreviewViewModel
    workspaceActive: root.isFeedbackWorkspace
}
```

### 2. `FeedbackWorkspace.qml` 不再用缓存子项 `visible` 判定进入页面

修改前：

```qml
viewModel.setWorkspaceActive(visible)
onVisibleChanged: if (viewModel) viewModel.setWorkspaceActive(visible)
onViewModelChanged: root.syncProfileDrafts()
```

修改后：

```qml
property bool workspaceActive: false

viewModel.setWorkspaceActive(workspaceActive)
onWorkspaceActiveChanged: if (viewModel) viewModel.setWorkspaceActive(workspaceActive)
onViewModelChanged: {
    root.syncProfileDrafts()
    if (viewModel) {
        viewModel.setWorkspaceActive(workspaceActive)
    }
}
```

### 3. `FeedbackWorkspace.qml` 进入页和消息出现时滚动到最新

修改前：

```qml
if (scrollToLatestAfterSubmit) {
    scheduleScrollToLatest("submitMessageCountChanged")
}
```

修改后：

```qml
Component.onCompleted: if (root.workspaceActive) scheduleScrollToLatest("chatCompleted")

Connections {
    target: root
    function onWorkspaceActiveChanged() {
        if (root.workspaceActive) {
            scheduleScrollToLatest("workspaceActivated")
        }
    }
}

if (scrollToLatestAfterSubmit) {
    scheduleScrollToLatest("submitMessageCountChanged")
} else if (scrollToLatestWhenAvailable || root.workspaceActive) {
    scheduleScrollToLatest("messageCountChanged")
}
```

## 验证结果

- `cmake --build --preset windows-msvc-release-real --config Release`：通过。
- `ctest --test-dir .\dit-tools-src\cinevault-pro\build\windows-msvc-release-real --output-on-failure`：8/8 通过。
- `.\tool\build_windows.ps1 -RealWorkflow`：通过。
- 安装包产物：`output/v0.1.123/CineVault-Setup-v0.1.123.exe`，大小约 167 MB。
- 构建 staging 临时目录已清理。

## 待办清单（未完成）

- 暂无代码待办。
- 建议安装 `v0.1.123` 后做一次人工界面回归：从其他页面切入“使用反馈”、重新进入软件后打开“使用反馈”、点击刷新后确认消息窗口停在最新消息。

## 下一步要做什么

- 如需发布，下一步可将本次修复提交并发布 `v0.1.123` 到 GitHub Release。
