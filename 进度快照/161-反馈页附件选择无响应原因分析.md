# 进度快照 161

- 时间：2026-07-01
- 已完成内容：已读取最新快照 `160-发布v0.1.106到github.md`；已完成“使用反馈”页附件无响应问题的桌面端/服务端链路排查；已确认问题主要不在服务端协议，而在桌面端本地附件选择与反馈展示链路。
- 当前修改模块：反馈系统桌面端附件选择问题分析模块

## 已完成内容

- 已确认服务端附件协议正常：
  - `services/feedback/server/app/main.py`
  - 客户端 `POST /api/client/messages`
  - 管理端 `POST /api/admin/conversations/{conversation_id}/messages`
  - 二者都复用同一个 `save_uploads()` 存储逻辑。

- 已确认桌面端接收附件链路正常：
  - `FeedbackService::parseMessage()` 会解析 `attachments`
  - `FeedbackMessageListModel` 会把附件映射给 QML
  - `FeedbackWorkspace.qml` 已能展示管理员发来的附件卡片

- 已确认桌面端发送附件的问题更像发生在“选中文件后进入待发送列表”之前：
  - `FeedbackWorkspace.qml` 中附件选择入口仍是：

```qml
onAccepted: if (viewModel) viewModel.addAttachmentUrls(selectedFiles)
```

  - `FeedbackViewModel::addAttachmentUrls()` 会对每个候选文件做本地路径转换与 `QFileInfo` 校验
  - 只要任一环节不满足，就会 `continue` 静默跳过，不会给用户任何失败提示

- 已确认当前反馈页还存在一处独立的 QML 运行时异常：

```text
qrc:/qt/qml/CineVault/src/ui/qml/workspaces/FeedbackWorkspace.qml:300: TypeError: Cannot read property 'width' of null
```

  - 该异常来自聊天气泡宽度绑定，说明本页在最近聊天 UI 改造后仍有未清理回归项。

## 代码前后对比

本模块当前阶段未修改业务源码，完成的是问题定位与根因收敛。

分析前：

```text
现象：桌面端反馈页可发文本，但选择附件后无提示、无待发送反馈；web 端却能正常发送附件给桌面端用户
```

分析后：

```text
结论1：服务端附件协议与存储逻辑正常，不是主因
结论2：桌面端接收附件展示正常，不是主因
结论3：最可疑点在 FileDialog -> addAttachmentUrls(selectedFiles) -> pendingAttachments 这一跳
结论4：当前页面还伴随 line 300 的 QML 宽度绑定异常，需要一并回归
```

## 待办清单（未完成）

- 先做本阶段源码备份并生成溯源文档
- 对 `FeedbackViewModel::addAttachmentUrls()` 做最小改动修复：
  - 强化 `selectedFiles` 到本地路径的兼容转换
  - 当 0 个文件被接纳时给出明确状态提示
- 修正 `FeedbackWorkspace.qml` 第 300 行附近的空父级宽度绑定错误
- 重新构建桌面端并人工验证：
  - 选择附件后立即出现待发送胶囊
  - 仅附件发送成功
  - 文本+附件发送成功
  - web 端能看到桌面端发来的附件
- 清理本轮构建临时缓存并生成下一份快照

## 下一步

- 下一步进入修复模块，优先改桌面端本地附件选择链路，再补 `FeedbackWorkspace.qml` 的运行时宽度异常并做完整回归。
