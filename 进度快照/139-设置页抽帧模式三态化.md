# 139-设置页抽帧模式三态化

日期：2026-07-01

## 已完成内容

- 已完成模块 4：设置页抽帧模式三态化。
- 已把抽帧模式从两态改成三态：
  - 每10帧抽1帧
  - 逐帧解析
  - 自定义间隔
- 已完成旧配置迁移逻辑：
  - 旧 `EveryNFrames + 10` 自动映射到“每10帧抽1帧”
  - 旧 `EveryNFrames + 非10` 自动映射到“自定义间隔”
- 已完成设置页联动：
  - 只有选择“自定义间隔”时才显示 `抽帧间隔`
- 已完成执行层联动：
  - 逐帧使用 `1`
  - 固定模式使用 `10`
  - 自定义模式使用用户输入值

## 当前修改到哪个模块

当前完成模块：004 设置页抽帧模式三态化。

## 具体修改的代码前后对比

### 修改前

```cpp
enum class AnalysisMode : qint32 {
    EveryNFrames = 0,
    EveryFrame
};
```

```qml
model: [
    { label: "每10帧抽1帧", value: 0 },
    { label: "逐帧解析", value: 1 }
]
```

### 修改后

```cpp
enum class AnalysisMode : qint32 {
    Every10Frames = 0,
    EveryFrame = 1,
    CustomInterval = 2
};
```

```qml
model: [
    { label: "每10帧抽1帧", value: 0 },
    { label: "逐帧解析", value: 1 },
    { label: "自定义间隔", value: 2 }
]
```

## 验证结果

- 遗留旧枚举检索：通过
- 运行态 UI 与抽帧烟测：待统一构建后验证

## 待办清单（未完成）

- 模块 5-7：反馈后端、开发者 Web、桌面反馈页
- 模块 8：构建、联调、部署、清理

## 下一步要做什么

下一步进入反馈系统主干，先建立新阶段源码备份，然后依次落 FastAPI 后端、开发者 Web 管理台和桌面端“使用反馈”页面。
