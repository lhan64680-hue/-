# 进度快照 081

- 时间：2026-06-27
- 已完成内容：新增“素材备份”顶部标签页，位置在“素材库”左侧，并接入三栏页面骨架。
- 当前修改模块：模块 1，导航与页面骨架。
- 阶段备份：
  - `backup/v0.1.66`
- 修改文件：
  - `dit-tools-src/cinevault-pro/CMakeLists.txt`
  - `dit-tools-src/cinevault-pro/src/app/AppContext.h`
  - `dit-tools-src/cinevault-pro/src/app/AppContext.cpp`
  - `dit-tools-src/cinevault-pro/src/shared/Formatters.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/ShellViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/ShellViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalShellViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalShellViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/qml/Main.qml`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialBackupWorkspace.qml`
  - `docs/素材备份功能开发方案.md`

## 已完成内容

- 顶部标签新增“素材备份”，顺序为 `项目库 / 素材备份 / 素材库 / 素材管理中心 / 报表 / 任务`。
- `WorkspaceId::Import` 不再归一到素材库，对外暴露为 `materialBackupWorkspaceId`。
- 新增 `MaterialBackupWorkspace.qml`，采用左源、中目的地、右任务的三栏布局骨架。
- 新增真实与 Minimal 两套素材备份 ViewModel 占位，并通过 `AppContext` 暴露 `materialBackupVm`。
- 素材备份页会隐藏全局源素材栏、右侧检查器和底部素材状态栏。
- CMake 已纳入新增 C++ 与 QML 文件。

## 当前修改到哪个模块

- 当前完成模块：模块 1，导航与页面骨架。
- 下一模块：模块 2，备份数据模型与规划器。

## 代码前后对比

### 1. 顶部标签顺序

修改前：

```cpp
QVariantMap{{QStringLiteral("label"), QStringLiteral("项目库")}, ...},
QVariantMap{{QStringLiteral("label"), QStringLiteral("素材库")}, ...},
QVariantMap{{QStringLiteral("label"), QStringLiteral("素材管理中心")}, ...},
```

修改后：

```cpp
QVariantMap{{QStringLiteral("label"), QStringLiteral("项目库")}, ...},
QVariantMap{{QStringLiteral("label"), QStringLiteral("素材备份")}, {QStringLiteral("value"), static_cast<int>(WorkspaceId::Import)}, ...},
QVariantMap{{QStringLiteral("label"), QStringLiteral("素材库")}, ...},
QVariantMap{{QStringLiteral("label"), QStringLiteral("素材管理中心")}, ...},
```

### 2. Import 工作区不再跳回素材库

修改前：

```cpp
const auto normalizedValue = (value == WorkspaceId::Import || value == WorkspaceId::Qc)
    ? WorkspaceId::Library
    : value;
```

修改后：

```cpp
const auto normalizedValue = value == WorkspaceId::Qc
    ? WorkspaceId::Library
    : value;
```

### 3. 主界面新增素材备份 Loader

修改前：

```qml
Loader {
    active: root.libraryWorkspaceLoaded
    visible: root.shellViewModel.currentWorkspace !== root.shellViewModel.projectLibraryWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialCenterWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.reportWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.jobsWorkspaceId
    sourceComponent: libraryWorkspaceComponent
}
```

修改后：

```qml
Loader {
    active: root.materialBackupWorkspaceLoaded
    visible: root.shellViewModel.currentWorkspace === root.shellViewModel.materialBackupWorkspaceId
    sourceComponent: materialBackupWorkspaceComponent
}

Loader {
    active: root.libraryWorkspaceLoaded
    visible: root.shellViewModel.currentWorkspace !== root.shellViewModel.projectLibraryWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialBackupWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.materialCenterWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.reportWorkspaceId
        && root.shellViewModel.currentWorkspace !== root.shellViewModel.jobsWorkspaceId
    sourceComponent: libraryWorkspaceComponent
}
```

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.66`。
- 最小 GUI 构建：`cmake --build --preset windows-msvc-release --config Release`，成功。
- 真实工作流构建：`cmake --build --preset windows-msvc-release-real --config Release`，成功。
- 首次在仓库根目录执行构建时因找不到 `CMakePresets.json` 失败，随后切换到 `dit-tools-src/cinevault-pro` 目录重跑成功；该失败是命令目录问题，不是代码问题。

## 待办清单（未完成）

- 模块 2：实现备份源、目的地、批次、目标任务、文件清单和校验模式等数据模型。
- 模块 2：实现路径规划、路径安全预检、空间预检和单测。
- 模块 3：实现真实拷贝、校验和日志。
- 模块 4：实现主目标级联拷贝与安全弹出。
- 模块 5：把页面按钮和列表接入真实 ViewModel。
- 模块 6：完成备份后的素材库归档联动。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材备份功能开发方案.md`。下一步从模块 2 开始，先执行 `bash tool/create_backup.sh` 创建下一阶段备份，再新增 `src/core/backup/BackupPlanner.*`、备份领域类型和 `tests/unit/BackupPlannerTest.cpp`。
