# 进度快照 060

- 时间：2026-06-26 17:20:00 +08:00
- 已完成内容：完成项目库管理服务层与索引一致性第一阶段开发，已创建阶段备份 `backup/v0.1.49`。
- 当前修改模块：项目库管理服务层模块。
- 阶段备份：已创建 `backup/v0.1.49`，后续会在本阶段收尾时补充 `backup/v0.1.49/项目库管理操作-前后对比.md`。
- 代码前后对比：
  - 修改前：`ProjectService` 只支持新建、打开、关闭和从项目库移除入口，无法真实重命名、移动或删除项目文件夹。
  - 修改后：`ProjectService` 新增 `renameProject`、`moveProject`、`deleteProjectToTrash`，支持同步更新项目数据库记录、项目库路径索引，并在当前项目操作时先关闭数据库后重开。
  - 修改前：项目目录只确保 `exports`、`logs`、`reports`，缓存/解析相关目录没有项目级结构。
  - 修改后：新建/打开项目会确保 `exports`、`logs`、`reports`、`cache/thumbnails`、`cache/report-preview`、`analysis/frames` 存在。
  - 修改前：`AppSettings` 只能新增/删除项目路径，移动或重命名后容易残留旧路径。
  - 修改后：`AppSettings` 新增 `replaceProjectPath`，并规范化最近项目与长期项目库路径，减少重复入口。
  - 修改前：素材管理中心全局索引没有项目移动/重命名后的引用修正入口。
  - 修改后：`GlobalDatabaseManager` 新增项目引用更新和移除接口，可同步 `project_registry`、`global_video_asset` 和 FTS 项目名。
- 验证结果：
  - 已运行局部 `git diff --check`，本模块修改未发现空白错误；仅输出既有 LF/CRLF 提示。
- 待办清单（未完成）：
  - 模块 2：把缩略图、报表预览、视频解析帧的新产物写入项目文件夹。
  - 模块 3：补齐项目库右键菜单和卡片按钮。
  - 模块 4：构建安装包、运行 `ctest`、启动烟测、清理临时缓存。
- 下一步要做什么：继续修改产物路径，让新生成的项目数据落入对应项目目录。
