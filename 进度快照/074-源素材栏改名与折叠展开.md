# 进度快照 074

- 时间：2026-06-27
- 已完成内容：左侧“素材源栏”改为“源素材”；标题左侧增加折叠/展开按钮；折叠后主布局宽度收窄为 56，并保留一个源素材图标和展开入口。
- 当前修改模块：模块 5，源素材栏改名与折叠展开。
- 阶段备份：
  - `backup/v0.1.59`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/ui/qml/Main.qml`
  - `dit-tools-src/cinevault-pro/src/ui/qml/components/SourceRail.qml`
  - `docs/素材库与素材管理体验优化方案.md`

## 代码前后对比

### 主布局宽度

修改前：

```qml
Layout.preferredWidth: ... ? 0 : 270
```

修改后：

```qml
property bool sourceRailCollapsed: false

Layout.preferredWidth: ... ? 0 : (root.sourceRailCollapsed ? 56 : 270)
collapsed: root.sourceRailCollapsed
onCollapseRequested: function(nextCollapsed) {
    root.sourceRailCollapsed = nextCollapsed
}
```

### 标题与按钮

修改前：

```qml
Text {
    text: "素材源栏"
}
```

修改后：

```qml
ActionButton {
    text: root.collapsed ? ">" : "<"
    onClicked: root.collapseRequested(!root.collapsed)
}

Text {
    visible: !root.collapsed
    text: "源素材"
}
```

## 验证结果

- 最小 GUI 构建：`cmake --build --preset windows-msvc-release --config Release`，通过。
- 真实工作流构建：`cmake --build --preset windows-msvc-release-real --config Release`，通过。
- 构建均通过项目 `tool/windows_toolchain.ps1` 的 `Invoke-VcVarsCommand` 执行。

## 待办清单（未完成）

- 模块 6：设置弹窗背景模糊。
- 模块 7：全局验证、打包与缓存清理。

## 下一步要做什么

执行模块 6 前创建下一阶段备份，然后在 `Main.qml` 中为设置弹窗打开状态增加主界面模糊和半透明遮罩。
