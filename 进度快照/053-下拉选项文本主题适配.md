# 进度快照 053

- 时间：2026-06-26 13:48:22 +08:00
- 已完成内容：修复所有基于 `ThemedComboBox` 的下拉选项文本不可见问题。下拉 delegate 不再只依赖 `model[control.textRole]`，而是通过 `optionText(rowData, rowModel)` 同时兼容 `model`、`modelData`、JS 对象、QVariantMap 和普通字符串值。下拉选项文本颜色统一使用 `Theme.text`，禁用态使用 `Theme.weak`，可随暗色/浅色主题自动适配。
- 当前修改模块：主题下拉组件模块，涉及 `ThemedComboBox.qml`。
- 阶段备份：已创建 `backup/v0.1.45`，并新增 `backup/v0.1.45/下拉选项文本主题适配-前后对比.md` 记录本阶段关键代码前后对比。
- 代码前后对比：
  - 修改前：delegate 文本为 `control.textRole.length > 0 ? model[control.textRole] : modelData`，在部分 QVariantList/QVariantMap 或 JS 对象模型中弹层选项取不到 `label`。
  - 修改后：新增 `optionText(rowData, rowModel)`，优先读取 `rowModel[role]`，再读取 `rowData[role]`，最后 fallback 到普通值字符串。
  - 修改前：普通下拉项文本颜色使用 `Theme.muted`，暗色弹层中可读性偏弱。
  - 修改后：可用态下拉项文本使用 `Theme.text`，禁用态使用 `Theme.weak`，跟随当前主题语义色。
- 验证结果：
  - 构建命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`
  - 最终产物：`output/v0.1.54/CineVault-Setup-v0.1.54.exe`
  - 安装包大小：`55252873`
  - SHA256：`F2F0D3397964DF3D4179CCC249143B179BE278F97AEA2EB99DEA9DADC0A946FD`
  - `ctest`：命令成功退出，当前构建目录输出 `No tests were found!!!`
  - 5 秒临时部署烟测：通过，状态 `SMOKE_RUNNING_AFTER_5S`，`NO_QML_LOG`
  - 临时缓存：空的 `output/_installer_staging` 已删除；`output/_smoke_runtime` 未残留。
- 待办清单（未完成）：
  - 安装或运行 `output/v0.1.54/CineVault-Setup-v0.1.54.exe` 后人工打开素材管理中心与设置页，复核全部下拉框弹层文字是否显示清晰。
  - 浅色/暗色/跟随系统三种主题下分别检查下拉项普通态、悬停态和当前值文本对比度。
- 下一步要做什么：人工复核下拉框视觉效果；如果仍有某个模型类型显示异常，再针对该模型补充一条读取路径。
