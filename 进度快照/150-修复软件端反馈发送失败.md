# 进度快照 150

- 时间：2026-07-01 12:02
- 已完成内容：修复桌面端“使用反馈”页无法发送反馈信息的问题，根因为 `FeedbackWorkspace.qml` 中跨 `Loader` 作用域引用 `draftMessageArea`。
- 当前修改模块：桌面端反馈发送失败修复

## 已完成内容

- 已从本地日志确认根因：

```text
qrc:/qt/qml/CineVault/src/ui/qml/workspaces/FeedbackWorkspace.qml:16: ReferenceError: draftMessageArea is not defined
```

- 已修复 `FeedbackWorkspace.qml`：
  - `root` 层发送函数改为显式接收文本参数
  - 发送成功后清空输入框的逻辑下沉回聊天组件内部
  - 按钮发送与回车发送都改为传入 `draftMessageArea.text`

## 代码前后对比

修改前：

```qml
function submitDraftMessage() {
    if (viewModel) {
        viewModel.sendMessage(draftMessageArea.text)
    }
}

Connections {
    target: viewModel
    function onMessageSubmitted(success) {
        if (success) {
            draftMessageArea.text = ""
        }
    }
}
```

修改后：

```qml
function submitDraftMessage(text) {
    if (viewModel) {
        viewModel.sendMessage(text)
    }
}

Connections {
    target: viewModel
    function onMessageSubmitted(success) {
        if (success) {
            draftMessageArea.text = ""
        }
    }
}
```

## 待办清单（未完成）

- 发布修复后的安装包到 GitHub
- 用修复版安装包人工复测桌面端反馈发送
- 验证旧版本客户端是否能检测到修复版更新

## 下一步

- 编译并发布 `v0.1.105`，让应用内自动更新直接落到修复后的版本。
