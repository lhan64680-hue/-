# 进度快照 068

- 时间：2026-06-26 19:04:12 +08:00
- 已完成内容：素材源栏的素材目录卡片右下角已增加“移除”按钮，点击后弹出确认弹窗；确认后只从当前项目移除素材源及关联索引记录，不删除磁盘原始文件。
- 当前修改模块：素材源栏素材目录卡片移除入口。
- 阶段备份：
  - `backup/v0.1.53`
  - 前后对比文档：`backup/v0.1.53/素材源卡片移除按钮与确认弹窗-前后对比.md`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/ui/qml/components/SourceCard.qml`
  - `dit-tools-src/cinevault-pro/src/ui/qml/components/SourceRail.qml`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/SourceRailViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/SourceRailViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalSourceRailViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalSourceRailViewModel.cpp`
  - `dit-tools-src/cinevault-pro/src/application/LibraryQueryService.h`
  - `dit-tools-src/cinevault-pro/src/application/LibraryQueryService.cpp`

## 代码前后对比

### 卡片按钮

修改前：

```qml
StatusChip {
    Layout.maximumWidth: parent.width
    label: statusLabel
    tint: statusColor
}
```

修改后：

```qml
RowLayout {
    Layout.fillWidth: true
    spacing: 8

    StatusChip {
        Layout.maximumWidth: parent.width - removeButton.width - 8
        label: statusLabel
        tint: statusColor
    }

    Item { Layout.fillWidth: true }

    ActionButton {
        id: removeButton
        Layout.preferredWidth: 54
        Layout.preferredHeight: 28
        text: "移除"
        danger: true
        textPixelSize: 12
        onClicked: root.removeRequested()
    }
}
```

### 确认弹窗与移除调用

修改前：素材源卡片只处理选择。

```qml
MouseArea {
    anchors.fill: parent
    onClicked: viewModel.selectSource(sourceId)
}
```

修改后：

```qml
onRemoveRequested: {
    root.pendingRemoveSourceId = sourceId
    root.pendingRemoveSourceName = model.name
    root.pendingRemoveSourcePath = model.path
    root.removeErrorText = ""
    confirmRemoveDialog.open()
}
```

```qml
ActionButton {
    text: "确认移除"
    danger: true
    onClicked: {
        if (viewModel && viewModel.removeSource(root.pendingRemoveSourceId)) {
            confirmRemoveDialog.close()
        } else {
            root.removeErrorText = "移除失败，请确认项目数据库仍可访问。"
        }
    }
}
```

### 服务层移除逻辑

修改前：无素材目录级移除方法。

修改后：

```cpp
bool LibraryQueryService::removeSourceRoot(qint64 sourceRootId)
{
    const QStringList statements = {
        QStringLiteral("DELETE FROM media_stream WHERE asset_id IN (SELECT id FROM asset_file WHERE source_root_id = ?)"),
        QStringLiteral("DELETE FROM media_metadata WHERE asset_id IN (SELECT id FROM asset_file WHERE source_root_id = ?)"),
        QStringLiteral("DELETE FROM thumbnail WHERE asset_id IN (SELECT id FROM asset_file WHERE source_root_id = ?)"),
        QStringLiteral("DELETE FROM folder_node WHERE source_root_id = ?"),
        QStringLiteral("DELETE FROM asset_file WHERE source_root_id = ?"),
        QStringLiteral("DELETE FROM source_root WHERE id = ?")
    };
    ...
    emit dataChanged();
    return true;
}
```

### 最小 GUI 兼容

修改前：`MinimalSourceRailViewModel` 没有 `removeSource(...)`，共享 QML 点击新按钮时会缺少同名方法。

修改后：

```cpp
bool MinimalSourceRailViewModel::removeSource(qint64 sourceRootId)
{
    ...
    reload();
    if (m_selectedSourceId == sourceRootId) {
        m_selectedSourceId = 0;
        emit selectionChanged();
    }
    emit sourceSelected(m_selectedSourceId);
    return true;
}
```

## 验证结果

- 完整构建命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`
- 完整构建结果：成功生成安装包 `output/v0.1.61/CineVault-Setup-v0.1.61.exe`。
- 安装包大小：`55291889` 字节。
- 安装包 SHA256：`482865804AA8C923057371E36EB6A269D6C4A8C73B6BA9D6BDEDE79103C3FD37`。
- 测试命令：`ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure`
- 测试结果：命令成功执行，当前构建目录提示 `No tests were found!!!`。
- 启动烟测：临时部署 Qt 运行库后隐藏启动 `CineVault.exe` 5 秒，进程未提前退出。
- 最小 GUI 构建验证：在 `dit-tools-src/cinevault-pro` 下执行 `cmake --preset windows-msvc-release` 与 `cmake --build --preset windows-msvc-release --config Release`，构建成功。
- 临时缓存清理：已删除 `output/_smoke_runtime` 和 `output/_installer_staging`。

## 待办清单（未完成）

- 使用真实项目人工复核素材源卡片“移除”按钮位置与点击热区。
- 使用真实项目人工复核取消/确认弹窗行为。
- 使用真实项目人工复核移除后素材源列表、素材列表、检查器和素材管理中心全局索引刷新结果。

## 下一步要做什么

如人工复核发现按钮视觉或刷新链路有偏差，再进入下一轮小范围修正。
