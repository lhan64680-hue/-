# 250 - 快捷搜索中性深灰主题完成

## 已完成内容

- 快捷搜索窗口改为固定中性深灰色板，与软件深色界面保持一致。
- 移除快捷搜索窗口对 `Theme.selectedBg`、`Theme.selectedLine`、`Theme.blue`、`Theme.orange` 的引用。
- 外框、搜索框、快捷键标签、结果选中状态、文件夹图标、结果排名和模型状态全部改为灰阶。
- 搜索逻辑、全局快捷键、结果分区、键盘操作和托盘行为未改动。
- UI 契约测试新增中性深灰主题断言，防止后续重新引入彩色强调样式。

## 修改模块

- `src/ui/qml/components/QuickSearchWindow.qml`
- `tests/unit/MaterialCenterUiContractTest.cpp`
- `docs/视觉模型辅助模糊搜索方案.md`

## 代码前后对比

```qml
// 修改前：跟随蓝色/紫色强调色
color: Theme.selectedBg
border.color: Theme.selectedLine
color: Theme.blue

// 修改后：快捷搜索专用中性深灰色板
readonly property color quickBg: "#0E1014"
readonly property color quickHeader: "#171A20"
readonly property color quickSelected: "#2A3039"
readonly property color quickAccent: "#C6CDD7"
```

## 验证结果

- QML 编译成功。
- `QuickSearchControllerTest` 通过。
- `MaterialCenterUiContractTest` 通过。
- 快捷搜索运行探针退出码 0，窗口状态：`windowFound=1 visible=1`。
- Windows MSVC Release 全量测试：28/28 通过，0 失败。
- `git diff --check` 通过。

## 构建产物

- 版本：`v0.1.152`
- 安装包：`G:\data\app\DIT-tools\output\v0.1.152\CineVault-Setup-v0.1.152.exe`
- 大小：187,179,532 字节。
- SHA-256：`6FFFC60F0003F235689F9A8B75C980DB67D7052EDE8D88381A10F34BB5EE4373`。

## 待办清单

- [x] 快捷搜索改为中性深灰主题。
- [x] 移除彩色强调色引用。
- [x] 增加样式回归契约。
- [x] 完成运行探针与 28 项全量测试。
- [x] 生成本地安装包。
- [ ] 用户安装 `v0.1.152` 后进行实际视觉体验确认。

## 下一步

安装 `v0.1.152`，按 `Alt+Space` 检查深灰快捷搜索窗。如需进一步微调，仅调整灰阶亮度、圆角或窗口尺寸，不需要改动搜索功能模块。
