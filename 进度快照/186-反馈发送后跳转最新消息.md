# 进度快照 186

- 时间：2026-07-02 11:37:37
- 当前模块：反馈页消息列表滚动

## 已完成内容

- 已读取最新快照：`进度快照/185-v0.1.120安装包已发布到GitHub.md`。
- 已完成阶段备份：
  - `backup/v0.1.144`
  - `backup/v0.1.144/反馈发送后跳转最新消息代码对比.md`
- 已定位反馈发送链路：
  - 服务层发送成功时先更新消息模型，再发出 `messageSubmitted(true)`。
  - Web 管理页发送后已有 `scrollTop = scrollHeight`，本次只调整桌面端反馈页。
- 已修改桌面端反馈页：
  - 文件：`dit-tools-src/cinevault-pro/src/ui/qml/workspaces/FeedbackWorkspace.qml`
  - 发送动作触发后标记 `scrollToLatestAfterSubmit`。
  - 消息数量变化或发送成功后调度滚动到底。
  - 使用短 Timer 多次执行 `forceLayout()` 与 `positionViewAtEnd()`，避免新消息布局尚未完成时跳转不到底。
- 已更新构建产物：
  - `output/v0.1.121/CineVault-Setup-v0.1.121.exe`
- 已清理构建临时目录：
  - `output/_installer_staging`

## 当前修改到哪个模块

- 桌面端反馈页消息列表滚动模块。

## 具体修改的代码前后对比

修改前：发送成功和消息数量变化只写调试日志，没有主动滚动到最新消息。

```qml
Connections {
    target: viewModel
    function onMessageSubmitted(success) {
        root.logFeedbackDebug("messageSubmitted", {
            success: success
        })
    }
}

onCountChanged: {
    root.logFeedbackDebug("messageListCountChanged", {
        count: messageListView ? messageListView.count : -1,
        contentHeight: messageListView ? messageListView.contentHeight : -1
    })
}
```

修改后：发送后保留待跳转标记，并在模型变化/发送成功后调度滚动到底。

```qml
property bool scrollToLatestAfterSubmit: false
property int scrollToLatestRetries: 0

function scheduleScrollToLatest(reason) {
    if (!messageListView || messageListView.count <= 0) {
        root.logFeedbackDebug("messageListScrollSkipped", {
            reason: reason,
            count: messageListView ? messageListView.count : -1
        })
        return
    }

    scrollToLatestRetries = 4
    messageScrollToLatestTimer.lastReason = reason
    messageScrollToLatestTimer.restart()
}
```

```qml
scrollToLatestAfterSubmit = true
root.viewModel.sendMessage(draftText)
```

```qml
if (success) {
    scheduleScrollToLatest("submitSuccess")
} else {
    scrollToLatestAfterSubmit = false
}
```

```qml
if (scrollToLatestAfterSubmit) {
    scheduleScrollToLatest("submitMessageCountChanged")
}
```

## 验证结果

- `cmake --build --preset windows-msvc-release-real`：通过。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure`：8/8 通过。
- `powershell -ExecutionPolicy Bypass -File .\tool\build_windows.ps1 -RealWorkflow`：通过。
- 安装包已生成：`output/v0.1.121/CineVault-Setup-v0.1.121.exe`。
- 构建临时目录 `output/_installer_staging` 已清理。

## 待办清单（未完成）

- 建议用 `output/v0.1.121/CineVault-Setup-v0.1.121.exe` 做一次安装版手动烟测：进入反馈页发送一条文字消息和一条附件消息，确认发送后均自动跳转到最新消息。
- 如需对外分发，需要继续发布 `v0.1.121` 到 GitHub Release。

## 下一步要做什么

- 进行安装版反馈发送烟测；确认无误后再发布 `v0.1.121`。

