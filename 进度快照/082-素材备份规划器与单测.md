# 进度快照 082

- 时间：2026-06-27
- 已完成内容：新增素材备份领域类型、备份规划器和规划器单测，完成路径与空间预检。
- 当前修改模块：模块 2，备份数据模型与规划器。
- 阶段备份：
  - `backup/v0.1.67`
- 修改文件：
  - `dit-tools-src/cinevault-pro/CMakeLists.txt`
  - `dit-tools-src/cinevault-pro/src/domain/Enums.h`
  - `dit-tools-src/cinevault-pro/src/domain/Entities.h`
  - `dit-tools-src/cinevault-pro/src/core/backup/BackupPlanner.h`
  - `dit-tools-src/cinevault-pro/src/core/backup/BackupPlanner.cpp`
  - `dit-tools-src/cinevault-pro/tests/unit/BackupPlannerTest.cpp`
  - `docs/素材备份功能开发方案.md`

## 已完成内容

- 新增 `BackupSourceKind`、`BackupVerificationMode`、`BackupTaskState` 和 `JobType::Backup`。
- 新增 `BackupSource`、`BackupDestination`、`BackupFileItem`、`BackupDestinationTask`、`BackupRequest`、`BackupPlan`。
- 新增 `BackupPlanner`，支持生成批次名、扫描文件/文件夹/卷、生成目标相对路径和每目的地任务。
- 规划器会阻止：无源、无目的地、源/目标不存在、不可读源、不可写目标、重复目的地、目标在源内部、源在目标内部、空间不足、目标相对路径冲突。
- 新增 `BackupPlannerTest`，覆盖批次名清洗、文件/文件夹收集、多目标计划路径、嵌套路径、重复目的地和空间不足。
- 启用 `include(CTest)`，并给 Windows 测试目标配置 Qt DLL 路径，解决 `ctest` 运行时报 `0xc0000135` 的问题。

## 当前修改到哪个模块

- 当前完成模块：模块 2，备份数据模型与规划器。
- 下一模块：模块 3，拷贝引擎与校验。

## 代码前后对比

### 1. 备份领域枚举

修改前：

```cpp
enum class JobType : qint32 {
    Scan = 0,
    Metadata,
    Thumbnail,
    GlobalSync,
    ContentAnalysis,
    Preview,
    Report,
    Export
};
```

修改后：

```cpp
enum class JobType : qint32 {
    Scan = 0,
    Metadata,
    Thumbnail,
    GlobalSync,
    ContentAnalysis,
    Preview,
    Report,
    Export,
    Backup
};

enum class BackupSourceKind : qint32 {
    File = 0,
    Directory,
    Volume
};
```

### 2. 规划器入口

修改前：

```text
src/core/backup/BackupPlanner.* 不存在。
```

修改后：

```cpp
class BackupPlanner {
public:
    BackupPlan buildPlan(const BackupRequest &request) const;

    static QString defaultBatchName(const QString &projectName, const QDateTime &timestamp = QDateTime::currentDateTime());
    static QString safePathSegment(const QString &name);
};
```

### 3. CTest 运行环境

修改前：

```cmake
if(BUILD_TESTING)
    enable_testing()
    find_package(Qt6 REQUIRED COMPONENTS Test)
    qt_add_executable(CineVaultSmokeTest tests/unit/SmokeTest.cpp)
endif()
```

修改后：

```cmake
include(CTest)

if(BUILD_TESTING)
    find_package(Qt6 REQUIRED COMPONENTS Test)
    qt_add_executable(CineVaultSmokeTest tests/unit/SmokeTest.cpp)
    qt_add_executable(BackupPlannerTest
        tests/unit/BackupPlannerTest.cpp
        src/core/backup/BackupPlanner.cpp
    )
    set_tests_properties(CineVaultSmokeTest BackupPlannerTest PROPERTIES
        ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${CINEVAULT_TEST_QT_BIN_DIR}"
    )
endif()
```

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.67`。
- 最小 GUI 构建：`cmake --build --preset windows-msvc-release --config Release`，成功。
- 真实工作流构建：`cmake --build --preset windows-msvc-release-real --config Release`，成功。
- 最小 GUI 测试：`ctest --test-dir build/windows-msvc-release --output-on-failure`，2/2 通过。
- 真实工作流测试：`ctest --test-dir build/windows-msvc-release-real --output-on-failure`，2/2 通过。
- 测试环境修复：首次启用 CTest 后最小 GUI 测试因找不到 Qt DLL 报 `0xc0000135`；已改为从 `Qt6::qmake` 推导 Qt bin 目录并写入 CTest PATH。

## 待办清单（未完成）

- 模块 3：实现真实文件复制、目标独立 worker、大小/SHA-256/MD5 校验和 `CineVaultBackupLog.json`。
- 模块 4：实现级联拷贝和源卷安全弹出。
- 模块 5：把页面源/目标/任务列表接入真实模型。
- 模块 6：完成备份完成后的素材库归档路径选择与导入。
- 模块 7：最终构建、测试、打包和临时缓存清理。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材备份功能开发方案.md`。下一步从模块 3 开始，先执行 `bash tool/create_backup.sh` 创建下一阶段备份，再新增 `BackupCopyEngine.*`，让它消费 `BackupPlan` 并输出每目标进度、校验结果和备份日志。
