# 进度快照 018

- 时间：2026-06-13 16:23:13
- 已完成内容：进入新功能阶段前已创建本地备份 `backup/v0.1.15`；修复打开/切换项目后旧搜索词、旧素材源筛选、旧类型筛选残留导致列表为空的问题；为旧项目数据库增加基础表与媒体表缺失字段兼容补列；素材库/检查器查询失败时写入日志，不再静默显示空；项目表打开时优先读取最新记录，创建项目记录前清理旧记录；`output/v0.1.15` 构建通过并完成短启动检查
- 当前修改模块：AppContext、ShellViewModel、SourceRailViewModel、LibraryWorkspaceViewModel、DatabaseManager、LibraryQueryService、ProjectService
- 待办清单：需要用户运行 `output/v0.1.15/CineVault.exe`，打开有数据的旧项目，如 `projects/001/project.cvdb` 或 `projects/撒旦/project.cvdb`，确认素材源栏和素材库不再显示为空；若仍为空，查看项目 `logs/app.log` 中的查询失败详情
- 下一步：实机打开旧项目验证；如发现更早版本字段名不同，再按日志补充字段映射迁移
