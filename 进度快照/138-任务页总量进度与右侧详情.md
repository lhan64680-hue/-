# 138-任务页总量进度与右侧详情

日期：2026-07-01

## 已完成内容

- 已完成模块 3：任务页总量进度与右侧详情。
- 已为 `VideoAnalysisService` 补齐批次汇总状态：
  - 总数
  - 已处理数
  - 成功数
  - 失败数
  - 排队数
  - 当前素材
  - 总体百分比
- 已升级 `JobTimelineViewModel`：
  - 同时消费 `JobService` 和 `VideoAnalysisService`
  - 支持选中任务
  - 支持右侧详情栏展示标题、状态、进度、更新时间、错误信息
- 已重构 `JobsWorkspace.qml`：
  - 左上显示总量进度卡片
  - 左下显示任务列表
  - 右侧显示具体任务状态反馈

## 当前修改到哪个模块

当前完成模块：003 任务页总量进度与右侧详情。

## 具体修改的代码前后对比

### 修改前

```cpp
emit analysisQueueChanged(m_currentVideoKey, m_analysisQueue.size());
```

```qml
ListView {
    Layout.fillWidth: true
    Layout.fillHeight: true
    model: viewModel.model
}
```

### 修改后

```cpp
emit analysisQueueChanged(m_currentVideoKey, m_analysisQueue.size());
notifyBatchChanged();
```

```qml
ColumnLayout {
    Rectangle { /* 总量进度卡片 */ }
    Rectangle { /* 任务列表 */ }
}

Rectangle {
    /* 右侧详情栏 */
}
```

## 验证结果

- 定向状态/字段检索：通过
- 运行态 UI 烟测：待统一构建后验证

## 待办清单（未完成）

- 模块 4：设置页抽帧模式三态化
- 模块 5-7：反馈后端、开发者 Web、桌面反馈页
- 模块 8：构建、联调、部署、清理

## 下一步要做什么

下一步进入模块 4，改抽帧模式枚举和设置页联动逻辑，补上“自定义间隔”模式，并处理旧设置迁移。
