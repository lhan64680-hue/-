# 进度快照 143

- 时间：2026-07-01 11:12:00
- 已完成内容：完成模块8联调、隔离端口部署、桌面端重编译与临时缓存清理；反馈系统最终改为独立端口 `3021` 对外提供服务。
- 当前修改模块：模块8 联调部署与清理（已完成）

## 代码前后对比

### 1. 桌面端反馈入口

修改前：

```cpp
constexpr auto kFeedbackServiceUrl = "http://115.231.35.105/dit-feedback/";
```

修改后：

```cpp
constexpr auto kFeedbackServiceUrl = "http://115.231.35.105:3021/";
```

### 2. PM2 支持隔离实例

修改前：

```js
const envPath = path.join(serverDir, ".env");
const envFile = loadEnvFile(envPath);

{
  name: "dit-feedback-server",
}
```

修改后：

```js
const envFileName = process.env.DIT_FEEDBACK_ENV_FILE || ".env";
const envPath = path.join(serverDir, envFileName);
const envFile = loadEnvFile(envPath);
const appName = process.env.DIT_FEEDBACK_APP_NAME || envFile.DIT_FEEDBACK_APP_NAME || "dit-feedback-server";

{
  name: appName,
}
```

### 3. 备份脚本补入 services

修改前：

```bash
if [[ -d installer ]]; then
  cp -a installer "$target/"
fi
```

修改后：

```bash
if [[ -d installer ]]; then
  cp -a installer "$target/"
fi

if [[ -d services ]]; then
  cp -a services "$target/"
fi
```

## 本轮联调与部署结果

- 新增服务器 PM2 进程：`dit-feedback-public`
- 新增公开访问地址：`http://115.231.35.105:3021/`
- 健康检查通过：`http://115.231.35.105:3021/health`
- 管理台首页通过：`http://115.231.35.105:3021/`
- 反馈主链路通过：会话初始化、用户发文本/附件、开发者登录、开发者回复、附件下载
- 离线补收通过：客户端重新拉取消息后可收到开发者回复
- 实时推送通过：WebSocket 收到 `message.created`
- 桌面端重编译通过：`output/v0.1.102/CineVault-Setup-v0.1.102.exe`
- 已清理本地 `services/feedback/server/storage`、`__pycache__`、临时发布目录、远端 `/tmp` 脚本

## 待办清单（未完成）

- 若后续允许，再清理此前尝试接入 `80` 端口时遗留的未使用配置
- 若需要正式分发，继续执行安装包发布与桌面端人工回归

## 下一步

- 以当前 `3021` 隔离实例为反馈正式入口，继续做桌面端人工体验回归或发布分发。
