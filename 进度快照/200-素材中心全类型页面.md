# 200-素材中心全类型页面

- 时间：2026-07-04
- 当前模块：模块 4，页面调整

## 已完成内容

- 已读取上一份快照：`进度快照/199-全字段搜索与筛选.md`。
- 已补充本模块代码对比文档：`backup/v0.1.154/素材中心全类型页面-代码对比.md`。
- `MaterialCenterListModel` 新增 `assetKey`、`assetType`、`assetTypeLabel`、`extension`、`canAnalyze`、`canConfirm` 等角色。
- `MaterialCenterViewModel` 新增素材类型筛选、素材详情字段、图片原图预览、文本正文预览和非视频逐帧区隐藏控制。
- 素材管理中心 QML 新增类型筛选，列表显示素材类型和扩展名，详情页显示类型、技术摘要、正文预览、路径信息。
- 页面按钮和状态文案从“视频解析”调整为“素材解析”；逐帧解析区仅视频素材显示。
- 继续保留旧 `selectedVideoKey`、`selectVideo`、`confirmVideo` 等兼容接口，新增页面代码统一优先使用 `assetKey`。
- 已确认用户新增要求：提交给视觉模型解析的图片会在内存中统一转换为 JPG data URL，原文件格式不变。

## 当前修改到哪个模块

- `dit-tools-src/cinevault-pro/src/ui/models/MaterialCenterListModel.h`
- `dit-tools-src/cinevault-pro/src/ui/models/MaterialCenterListModel.cpp`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.h`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.cpp`
- `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialCenterWorkspace.qml`
- `dit-tools-src/cinevault-pro/src/ui/qml/components/JobProgressInspectorPane.qml`
- `dit-tools-src/cinevault-pro/src/application/VideoAnalysisService.cpp`

## 具体修改的代码前后对比

见 `backup/v0.1.154/素材中心全类型页面-代码对比.md`。

## 验证结果

- `git diff --check -- <模块4相关文件>`：通过，仅有既有 LF/CRLF 提示。
- `rg` 检查素材中心 QML 与进度面板中的旧“视频摘要/视频解析/选择左侧视频”文案：无残留。

## 待办清单（未完成）

- 模块 5：补测试。
- 模块 5：跑全量 `git diff --check`。
- 模块 5：Release 构建。
- 模块 5：`ctest --output-on-failure -C Release`。
- 模块 5：生成新版安装包。
- 模块 5：清理临时缓存并写最终快照。

## 下一步要做什么

- 进入模块 5，先查看现有测试结构和构建脚本，补素材中心同步/搜索测试与文本提取测试，再执行构建、测试和打包验证。
