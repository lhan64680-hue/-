# 进度快照 055

- 时间：2026-06-26 14:47:30 +08:00
- 已完成内容：规范化素材源栏目录卡片高度，修复“正常”等状态徽标被挤出卡片区域的问题。卡片自身新增 `implicitHeight: 124`，列表项高度改为跟随卡片高度加上下外边距计算。
- 当前修改模块：素材源栏目录卡片布局模块，涉及 `SourceCard.qml`、`SourceRail.qml`。
- 阶段备份：已创建 `backup/v0.1.47`，并新增 `backup/v0.1.47/素材源卡片状态布局规范化-前后对比.md` 记录本阶段关键代码前后对比。
- 代码前后对比：
  - 修改前：`SourceRail` delegate 固定 `height: 124`，卡片 `anchors.margins: 14` 后实际高度不足，标题、两行统计信息和 `StatusChip` 垂直叠加时会把“正常”徽标挤出卡片。
  - 修改后：`SourceCard` 声明 `implicitHeight: 124`，`SourceRail` delegate 使用 `height: sourceCard.implicitHeight + 28`，确保卡片实际高度足够并保留上下 `14px` 外边距。
  - 修改前：`SourceCard` 没有明确自己的高度规格，外层列表项容易写死压缩内容。
  - 修改后：卡片高度规格由卡片组件自身维护，外层只负责根据规格留出空间。
- 验证结果：
  - 构建命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`
  - 最终产物：`output/v0.1.56/CineVault-Setup-v0.1.56.exe`
  - 安装包大小：`55258026`
  - SHA256：`0BF54A3ED1F1A23623705E97E9D3E7E047936A15937B1A6EB32AAC9A190408F6`
  - `ctest`：命令成功退出，当前构建目录输出 `No tests were found!!!`
  - 5 秒临时部署烟测：通过，状态 `SMOKE_RUNNING_AFTER_5S`，`NO_QML_LOG`
  - 临时缓存：`output/_smoke_runtime` 未残留；空的 `output/_installer_staging` 已删除。
- 待办清单（未完成）：
  - 安装或运行 `output/v0.1.56/CineVault-Setup-v0.1.56.exe` 后人工复核素材源栏目录卡片，“正常”徽标应完整位于卡片内部。
  - 在 100%、125%、150% 显示缩放下检查素材源栏卡片间距和列表滚动观感。
- 下一步要做什么：人工复核实际 UI；如果状态徽标仍显得过低，再只微调 `SourceCard` 内部间距或卡片高度，不改共用 `StatusChip`。
