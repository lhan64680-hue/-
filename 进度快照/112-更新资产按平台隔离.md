# 112-更新资产按平台隔离

日期：2026-06-28

## 已完成内容

- 已为更新服务新增平台识别逻辑，Windows 与 macOS 现在分别匹配各自的 GitHub Release 资产。
- 已将更新缓存目录切分为平台子目录，避免不同平台已下载更新包互相影响。
- 已为待安装更新读取逻辑增加平台资产名校验，防止错误复用另一平台的安装包。
- 已给非 Windows 平台的“立即更新”增加安全兜底提示，避免误走 Windows PowerShell 安装脚本。
- 已将更新确认弹窗文案从“安装包”调整为更通用的“更新包”。
- 已基于本次修改重新构建真实工作流安装包，输出版本更新到 `v0.1.92`。

## 当前修改到哪个模块

当前完成到新增子模块：更新资产按平台隔离。

修改文件：
- `dit-tools-src/cinevault-pro/src/application/UpdateService.h`
- `dit-tools-src/cinevault-pro/src/application/UpdateService.cpp`
- `dit-tools-src/cinevault-pro/tests/unit/UpdateServiceTest.cpp`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/SettingsViewModel.cpp`
- `backup/v0.1.95/模块12-更新资产按平台隔离对比.md`
- `output/v0.1.92/CineVault-Setup-v0.1.92.exe`

## 具体修改的代码前后对比

### 平台资产匹配

修改前：

```cpp
return normalized.isEmpty() ? QString() : QStringLiteral("CineVault-Setup-%1.exe").arg(normalized);
```

修改后：

```cpp
if (normalizedPlatform == QStringLiteral("windows")) {
    return {QStringLiteral("CineVault-Setup-%1.exe").arg(normalized)};
}
if (normalizedPlatform == QStringLiteral("macos")) {
    return {
        QStringLiteral("CineVault-macOS-%1.dmg").arg(normalized),
        QStringLiteral("CineVault-macOS-%1.pkg").arg(normalized)
    };
}
```

### 下载缓存按平台隔离

修改前：

```cpp
m_downloadTargetPath = QDir(Paths::updatesRoot()).filePath(release.installerName);
```

修改后：

```cpp
m_downloadTargetPath = QDir(platformUpdatesRoot).filePath(release.installerName);
```

### 非 Windows 自动安装兜底

新增：

```cpp
#if !defined(Q_OS_WIN)
    if (errorMessage) {
        *errorMessage = QStringLiteral("当前平台暂未实现自动安装，请手动打开已下载的更新包：%1")
                            .arg(QDir::toNativeSeparators(installerPath));
    }
    return false;
#endif
```

## 验证结果

- 增量编译通过。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure` 通过。
- `UpdateServiceTest` 已覆盖 Windows / macOS 资产选择分支。
- 真实工作流安装包已生成：`output/v0.1.92/CineVault-Setup-v0.1.92.exe`
- `output/_installer_staging/v0.1.92` 已自动清理。

## 待办清单（未完成）

- 若后续要正式支持 macOS 自动安装，还需要补 macOS 的安装执行链路。
- 若 GitHub Release 最终采用不同的 macOS 资产命名规则，需要同步调整当前匹配规则。

## 下一步要做什么

- 可以继续把本次更新逻辑和当前 `v0.1.91` 安装包一起发布到 GitHub Release。
- 也可以继续补 macOS 自动安装闭环，完成跨平台更新体验收口。
