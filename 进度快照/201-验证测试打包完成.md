# 201-验证测试打包完成

- 时间：2026-07-04
- 当前模块：模块 5，验证、打包、清理

## 已完成内容

- 已读取上一份快照：`进度快照/200-素材中心全类型页面.md`。
- 已补充本模块代码对比文档：`backup/v0.1.154/验证测试打包-代码对比.md`。
- 新增 `DocumentPreviewServiceTest`，覆盖 TXT/MD/JSON/CSV 文本提取、大文本截断、PDF 和旧版 Office metadata-only 行为。
- 新增 `MaterialCenterQueryServiceTest`，覆盖全类型素材返回、全字段搜索、类型筛选和详情字段读取。
- 已把新增测试接入 `CMakeLists.txt` 和 Windows PATH aware 测试列表。
- 已完成 Release + FFmpeg 构建，并生成新版安装包。
- 已执行全量 `ctest`，10 个测试全部通过。
- 已执行全量 `git diff --check`，检查通过，仅有工作区 LF/CRLF 换行策略提示。
- 已清理空的安装包 staging 临时目录，测试数据库无残留。
- 已再次确认用户新增要求：提交给视觉模型解析的图片会在内存中统一转换为 JPG data URL，原文件图片格式不变。

## 当前修改到哪个模块

- `dit-tools-src/cinevault-pro/CMakeLists.txt`
- `dit-tools-src/cinevault-pro/tests/unit/DocumentPreviewServiceTest.cpp`
- `dit-tools-src/cinevault-pro/tests/unit/MaterialCenterQueryServiceTest.cpp`

## 具体修改的代码前后对比

见 `backup/v0.1.154/验证测试打包-代码对比.md`。

## 验证结果

- `git diff --check`：通过，仅有 LF/CRLF 提示。
- `powershell -ExecutionPolicy Bypass -File .\tool\build_windows.ps1 -Configuration Release -EnableFfmpeg`：通过。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-ffmpeg --output-on-failure -C Release`：10/10 通过。
- 最新安装包：`G:\data\app\DIT-tools\output\v0.1.126\CineVault-Setup-v0.1.126.exe`。
- 临时清理：已删除空目录 `G:\data\app\DIT-tools\output\_installer_staging`；未发现 `material-center.sqlite` 测试数据库残留。

## 待办清单（未完成）

- 无代码侧待办。
- 建议人工验收：在同一个项目中导入视频、JPG/PNG/WebP、TXT/MD/JSON/CSV/DOCX/XLSX/PPTX、音频、工程文件、压缩包，验证素材中心显示、搜索、解析和确认流程。

## 下一步要做什么

- 本轮开发已完成。后续可以直接安装 `output/v0.1.126/CineVault-Setup-v0.1.126.exe` 进行人工验收。
