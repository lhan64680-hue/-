# 134-素材管理中心默认排序改为文件名升序

日期：2026-06-30

## 已完成内容

- 已读取上一轮最新快照：`133-v0.1.98安装版自动化复测受阻记录.md`。
- 已确认本轮只处理一个模块：
  - 素材管理中心默认排序模块
- 已定位根因：
  - `MaterialCenterQueryService::fetchAssets()` 默认使用 `g.updated_at DESC`
  - 解析任务/同步任务更新素材记录后，最近更新的素材会自动跳到第一位
- 已完成最小范围修复：
  - 将素材管理中心默认排序改为 `file_name` 升序
  - 增加 `project_name`、`relative_path`、`video_key` 作为稳定补充排序键
- 已完成源码阶段备份：
  - 备份目录：`G:\data\app\DIT-tools\backup\v0.1.111`
  - 差异文档：`素材管理中心默认文件名升序-前后对比.md`
- 已完成真实工作流构建与打包：
  - 生成安装包：`G:\data\app\DIT-tools\output\v0.1.99\CineVault-Setup-v0.1.99.exe`
  - SHA256：`5913B9B229D8952D27AE3C278DC56ED698DBFD35B3BD0BE6BE48884528D06087`
- 已完成本机安装版升级：
  - 当前安装版：`影资管家 v0.1.99`
  - 安装路径：`D:\Program Files\影资管家\`

## 当前修改到哪个模块

当前完成模块：006 素材管理中心默认排序修复模块。

本轮没有扩散修改到项目素材列表排序，也没有改动筛选、解析、数据库结构或 UI 交互，只修复素材管理中心“按最近更新时间跳到最前”的默认排序行为。

## 具体修改的代码前后对比

### 修改前

```cpp
sql += QStringLiteral(" ORDER BY g.updated_at DESC, g.project_name COLLATE NOCASE, g.file_name COLLATE NOCASE LIMIT 2000");
```

### 修改后

```cpp
sql += QStringLiteral(
    " ORDER BY g.file_name COLLATE NOCASE ASC, "
    "g.project_name COLLATE NOCASE ASC, "
    "g.relative_path COLLATE NOCASE ASC, "
    "g.video_key ASC LIMIT 2000");
```

## 验证结果

- `MaterialCenterQueryService.cpp` 新排序语句存在：通过
- 真实工作流构建：通过
- 安装包打包：通过
- 本机安装版升级到 `v0.1.99`：通过
- 静态排序验证：通过
  - 当前全局库按新规则查询时，头部结果已按文件名升序返回，例如：
  - `01_MARKS_Soma_DENIM-15s_16x9_EDIT30_DC.mp4`
  - `2024JORYAxContinew秋季度假-广告片-服装-时尚视频-新片场.mp4`
  - `23-24 Renaissance Collection- Lilly&Lime.mp4`

## 待办清单（未完成）

- 在 `v0.1.99` 安装版内人工确认素材管理中心列表默认顺序
- 继续人工复测素材管理中心解析主路径
- 如果你还希望项目素材列表也改成文件名升序，下一轮单独处理该模块

## 下一步要做什么

下一步直接在本机 `v0.1.99` 安装版里打开素材管理中心确认默认排序。如果你还想把项目素材列表默认排序也统一成文件名升序，下一轮从这份 `134` 快照继续，只改那一个模块。
