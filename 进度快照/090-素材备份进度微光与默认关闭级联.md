# 进度快照 090

- 时间：2026-06-27
- 已完成内容：完成素材备份页面功能调整：备份执行中进度条新增微光动效，级联备份默认关闭。
- 当前修改模块：素材备份页面交互与默认设置模块。
- 阶段备份：
  - `backup/v0.1.74`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/domain/Entities.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialBackupWorkspace.qml`
  - `backup/v0.1.74/素材备份进度微光与默认关闭级联-前后对比.md`
  - `进度快照/089-素材备份默认关闭级联.md`
  - `进度快照/090-素材备份进度微光与默认关闭级联.md`

## 已完成内容

- 读取最新快照 `088-视觉接口JSON容错修复.md`，确认上一轮从视觉接口修复结束。
- 创建阶段备份 `backup/v0.1.74`。
- 模块 1：将素材备份级联默认值统一改为关闭：
  - `BackupRequest::cascadeEnabled`
  - `BackupPlan::cascadeEnabled`
  - `MaterialBackupViewModel::m_cascadeEnabled`
  - `MinimalMaterialBackupViewModel::m_cascadeEnabled`
  - `MaterialBackupWorkspace.qml` 中无 viewModel 时的 fallback checked 值
- 模块 1 完成后生成中间快照 `089-素材备份默认关闭级联.md`。
- 模块 2：在素材备份页新增局部 `GlowProgressBar`：
  - 整体进度条在 `viewModel.running` 时显示微光扫动。
  - 单任务进度条在任务状态为“复制中”或“校验中”时显示微光扫动。
  - 非运行态不持续动画。
- 完成真实工作流 Release 构建、CTest、安装包打包、5 秒启动烟测和空 staging 清理。

## 当前修改到哪个模块

- 当前完成模块：素材备份页面交互与默认设置模块。
- 下一模块：等待真实备份任务人工复核。

## 代码前后对比

### 1. 默认关闭级联

修改前：

```cpp
bool cascadeEnabled = true;
bool m_cascadeEnabled = true;
```

```qml
checked: viewModel ? viewModel.cascadeEnabled : true
```

修改后：

```cpp
bool cascadeEnabled = false;
bool m_cascadeEnabled = false;
```

```qml
checked: viewModel ? viewModel.cascadeEnabled : false
```

### 2. 整体进度微光

修改前：

```qml
ThemedProgressBar {
    value: viewModel ? viewModel.overallProgress : 0
}
```

修改后：

```qml
GlowProgressBar {
    value: viewModel ? viewModel.overallProgress : 0
    glowActive: viewModel && viewModel.running
}
```

### 3. 任务进度微光

修改前：

```qml
ThemedProgressBar {
    value: model.progress
}
```

修改后：

```qml
GlowProgressBar {
    value: model.progress
    glowActive: viewModel && viewModel.running && (model.state === 1 || model.state === 2)
}
```

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.74`。
- 真实工作流 Release 构建：成功。
- 真实工作流 CTest：3/3 通过。
- 真实工作流打包：成功生成 `output/v0.1.68/CineVault-Setup-v0.1.68.exe`。
- 安装包大小：52.93 MB。
- 安装包 SHA256：`EEAFF24C5E2A5AE365E316CA32E0A05E19A76ECDA978F39F0F1B42CFB342D0AC`。
- 启动烟测：真实工作流 `CineVault.exe` 启动 5 秒后手动结束，未提前退出。
- 临时缓存清理：已删除空的 `output/_installer_staging`。

## 待办清单（未完成）

- 人工打开素材备份页，确认“级联拷贝”默认未选中。
- 人工执行一次真实备份，确认整体进度条和任务进度条在复制/校验时出现微光流动。
- 人工开启“级联拷贝”后执行一次多目的地备份，确认级联路径仍正常。

## 下一步要做什么

安装或运行 `output/v0.1.68/CineVault-Setup-v0.1.68.exe`，使用小体积素材和两个目的地做真实备份复核。
