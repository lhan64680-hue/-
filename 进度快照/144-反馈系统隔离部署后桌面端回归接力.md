# 进度快照 144

- 时间：2026-07-01 11:18:37
- 已完成内容：完成反馈系统独立端口 `3021` 隔离部署、HTTP 与 WebSocket 联调、桌面端默认反馈地址切换、安装包重新构建；开始桌面端最终人工回归并确认当前安装目录仍是旧版。
- 当前修改模块：模块8后收尾验收与桌面端回归接力

## 代码前后对比

### 1. 桌面端反馈入口继续改为独立端口

修改前：

```cpp
constexpr auto kFeedbackServiceUrl = "http://115.231.35.105:3020/";
```

修改后：

```cpp
constexpr auto kFeedbackServiceUrl = "http://115.231.35.105:3021/";
```

### 2. PM2 配置支持按环境文件拉起隔离实例

修改前：

```js
const envPath = path.join(serverDir, ".env");
const envFile = loadEnvFile(envPath);
const feedbackHost = envFile.DIT_FEEDBACK_HOST || "127.0.0.1";
const feedbackPort = envFile.DIT_FEEDBACK_PORT || "3020";

{
  name: "dit-feedback-server",
}
```

修改后：

```js
const envFileName = process.env.DIT_FEEDBACK_ENV_FILE || ".env";
const envPath = path.join(serverDir, envFileName);
const envFile = loadEnvFile(envPath);
const appName = process.env.DIT_FEEDBACK_APP_NAME || envFile.DIT_FEEDBACK_APP_NAME || "dit-feedback-server";
const feedbackHost = envFile.DIT_FEEDBACK_HOST || "127.0.0.1";
const feedbackPort = envFile.DIT_FEEDBACK_PORT || "3020";

{
  name: appName,
}
```

## 本轮已确认结果

- 反馈公开实例在线：
  - PM2 进程：`dit-feedback-public`
  - 监听端口：`0.0.0.0:3021`
  - 地址：[http://115.231.35.105:3021/](http://115.231.35.105:3021/)
- 联调通过：
  - `GET /health`
  - 管理台首页
  - 客户端建会话
  - 客户端发文本/附件
  - 开发者登录与回复
  - 附件下载
  - 离线补拉
  - WebSocket 实时收到 `message.created`
- 桌面端产物已更新：
  - [CineVault-Setup-v0.1.102.exe](G:/data/app/DIT-tools/output/v0.1.102/CineVault-Setup-v0.1.102.exe)

## 当前卡点

- 本地安装目录应用 `D:\Program Files\影资管家\CineVault.exe` 仍是旧时间戳：
  - `2026/6/30 17:26:56`
- 旧安装版辅助树里没有“使用反馈”入口，说明不能拿它作为最终验收对象。
- 已确认代码中没有明显单实例门禁：
  - `src/app/main.cpp`
  - `src/app/AppBootstrap.cpp`
  - `src` 下未找到 `QLockFile / QSharedMemory / already running` 之类逻辑
- 尝试直接启动当前构建版：
  - `G:\data\app\DIT-tools\dit-tools-src\cinevault-pro\build\windows-msvc-release-real\CineVault.exe`
  - 启动时曾出现构建版进程，但没有形成可见主窗口供自动化抓取
  - 当前中断后只剩旧安装版后台进程：

```text
14528 D:\Program Files\影资管家\CineVault.exe
17612 D:\Program Files\影资管家\CineVault.exe
36224 D:\Program Files\影资管家\CineVault.exe
```

## 待办清单（未完成）

- 确认为什么构建目录新版进程未形成可见主窗口
- 在新版界面上完成最终桌面端回归：
  - 使用反馈页
  - 任务页
  - 设置页
  - 缩略图/预览相关回归
- 如有必要，再执行本地安装版覆盖安装或新版可执行文件直启验证

## 下一步

- 新窗口接力后优先处理“新版 `CineVault.exe` 起进程但未形成可见窗口”的原因，再继续桌面端最终验收。
