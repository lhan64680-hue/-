# 113-发布v0.1.92测试版

日期：2026-06-28

## 已完成内容

- 已将当前包含 `webp` 缩略图支持与跨平台更新资产隔离的源码整体提交到 `main`。
- 已推送远端分支：`origin/main`。
- 已创建并推送 Git 标签：`v0.1.92`。
- 已创建 GitHub 正式 Release：`CineVault v0.1.92 测试版`。
- 已上传 Windows 测试安装包：`CineVault-Setup-v0.1.92.exe`。

## 当前修改到哪个模块

当前完成到新增子模块：发布 `v0.1.92` 测试版。

关联文件：
- `backup/v0.1.96/模块13-发布v0.1.92测试版对比.md`
- `backup/v0.1.96/v0.1.92-release-notes.md`

关联产物：
- `output/v0.1.92/CineVault-Setup-v0.1.92.exe`
- `https://github.com/luojiang419/dit-tools/releases/tag/v0.1.92`

## 具体修改的代码前后对比

本模块不新增业务代码，主要完成源码同步与发布状态切换。

### 发布前

```text
- GitHub latest release：v0.1.90
- v0.1.92 安装包仅存在于本地 output/v0.1.92
- 当前改动尚未提交到 origin/main
```

### 发布后

```text
- 当前源码已提交：eac49463580155d869d3cb89abed62d7815774b0
- origin/main 已同步最新代码
- GitHub Release 已切换到 v0.1.92
- Windows 安装包已可通过 Release 页面下载
```

## 验证结果

- `git status --short --branch` 已恢复干净状态。
- `gh release view v0.1.92` 确认 Release 已发布，且不是 `draft` / `prerelease`。
- Release 资产 `CineVault-Setup-v0.1.92.exe` 状态为 `uploaded`。

## 待办清单（未完成）

- 等待手工测试 `v0.1.92` 的安装、启动、自动检查更新和 `webp` 缩略图显示。
- 若测试发现问题，继续基于当前 `main` 修复并重新构建下一版安装包。
- 若后续需要 macOS 测试包，还需要单独产出对应平台资产并上传到同版本 Release。

## 下一步要做什么

- 先下载并安装 `v0.1.92` 测试版进行验证。
- 重点回归：
  - `webp` 缩略图是否正常显示
  - 软件启动后的 GitHub 自动检查更新是否正常
  - Windows / macOS 资产是否各自匹配、互不干扰
