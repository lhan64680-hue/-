# 进度快照 147

- 时间：2026-07-01 11:47
- 已完成内容：完成反馈系统输入快捷键模块，Web 管理台与桌面端“使用反馈”页均改为 `Enter` 发送、`Ctrl+Enter` 换行。
- 当前修改模块：反馈输入快捷键支持

## 已完成内容

- Web 管理台回复输入框：
  - 普通 `Enter` 直接发送
  - `Ctrl+Enter` 保留换行输入
  - 输入法组合输入期间不抢按键
- 桌面端“使用反馈”输入框：
  - 普通 `Enter` 直接发送
  - `Ctrl+Enter` 保留换行输入
  - 输入法组合输入期间不抢按键
- Web 端已通过 `node --check services/feedback/admin/app.js` 语法检查。

## 代码前后对比

### 1. Web 管理台回车行为

修改前：

```js
elements.sendReply.addEventListener("click", async () => {
  try {
    await sendReply();
  } catch (error) {
    setConnectionStatus(error.message || "发送回复失败", true);
  }
});

elements.replyText.addEventListener("keydown", async (event) => {
  if ((event.ctrlKey || event.metaKey) && event.key === "Enter") {
    event.preventDefault();
    try {
      await sendReply();
    } catch (error) {
      setConnectionStatus(error.message || "发送回复失败", true);
    }
  }
});
```

修改后：

```js
async function submitReplyFromComposer() {
  try {
    await sendReply();
  } catch (error) {
    setConnectionStatus(error.message || "发送回复失败", true);
  }
}

elements.sendReply.addEventListener("click", submitReplyFromComposer);

elements.replyText.addEventListener("keydown", async (event) => {
  if (event.isComposing) {
    return;
  }

  if ((event.key === "Enter" || event.key === "NumpadEnter")
      && !event.ctrlKey
      && !event.metaKey
      && !event.shiftKey
      && !event.altKey) {
    event.preventDefault();
    await submitReplyFromComposer();
  }
});
```

### 2. 桌面端反馈输入框回车行为

修改前：

```qml
ActionButton {
    text: viewModel && viewModel.sending ? "发送中..." : "发送反馈"
    onClicked: if (viewModel) viewModel.sendMessage(draftMessageArea.text)
}
```

修改后：

```qml
function submitDraftMessage() {
    if (viewModel) {
        viewModel.sendMessage(draftMessageArea.text)
    }
}

TextArea {
    id: draftMessageArea
    Keys.onPressed: function(event) {
        if (draftMessageArea.inputMethodComposing) {
            return
        }
        if ((event.key === Qt.Key_Return || event.key === Qt.Key_Enter)
                && !(event.modifiers & Qt.ControlModifier)
                && !(event.modifiers & Qt.ShiftModifier)
                && !(event.modifiers & Qt.AltModifier)
                && !(event.modifiers & Qt.MetaModifier)) {
            event.accepted = true
            root.submitDraftMessage()
        }
    }
}

ActionButton {
    text: viewModel && viewModel.sending ? "发送中..." : "发送反馈"
    onClicked: root.submitDraftMessage()
}
```

## 待办清单（未完成）

- 编译新的 Windows 安装包
- 在构建产物中验证桌面端 QML 编译通过
- 推送 `main`
- 创建新版本 tag
- 创建 GitHub Release 并上传安装包
- 补写本阶段 `backup/v0.1.116` 前后对比溯源文档

## 下一步

- 进入构建与发布模块，生成新版本安装包并发布到 GitHub，供旧版本自动更新读取。
