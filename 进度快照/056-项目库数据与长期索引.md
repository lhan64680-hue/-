# 进度快照 056

- 时间：2026-06-26 16:21:48 +08:00
- 已完成内容：完成项目库数据层基础，实现长期项目索引 `knownProjects`、项目卡片摘要读取、项目列表模型和项目库 ViewModel。
- 当前修改模块：项目库数据与状态模块，涉及 `Entities.h`、`AppSettings.h/.cpp`、`ProjectService.h/.cpp`、`ProjectLibraryListModel.h/.cpp`、`ProjectLibraryViewModel.h/.cpp`、`CMakeLists.txt`。
- 阶段备份：已创建 `backup/v0.1.48`，并新增 `backup/v0.1.48/项目库与标题栏主题适配-前后对比.md` 作为本阶段溯源文档。
- 代码前后对比：
  - 修改前：项目仅通过 `recentProjects` 保留最近 10 条路径，没有长期项目库索引，无法长期展示所有新建/打开过的项目。
  - 修改后：新增 `knownProjects` 长期索引；新建/打开项目时同时写入 `knownProjects` 和 `recentProjects`，项目库展示时先按最近项目排序，再补齐长期索引中的其余项目。
  - 修改前：`ProjectService` 只维护当前项目，不能为项目库批量读取项目名、路径、创建时间和缺失状态。
  - 修改后：`ProjectService::projectLibraryEntries()` 会逐个临时读取项目数据库摘要，缺失项目保留为“缺失”状态，不影响当前项目连接。
  - 修改前：QML 没有项目卡片列表模型或项目库动作入口。
  - 修改后：新增 `ProjectLibraryListModel` 与 `ProjectLibraryViewModel`，支持搜索、新建项目、打开外部项目、点击项目卡片打开、从项目库移除入口。
- 待办清单（未完成）：
  - 新增「项目库」工作区 UI，并接入顶部标签。
  - 点击项目卡片后切换到「素材库」。
  - 项目库工作区隐藏素材源栏、检查器和底部任务栏。
  - 实现 Windows 原生标题栏主题适配。
  - 构建真实流程安装包、运行验证并清理临时缓存。
- 下一步要做什么：实现项目库 UI、导航顺序和工作区联动。

