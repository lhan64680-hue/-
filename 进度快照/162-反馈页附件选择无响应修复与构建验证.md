# 进度快照 162

- 时间：2026-07-01 13:45
- 已完成内容：已完成“使用反馈”页附件选择无响应修复；补齐附件选择失败提示；修复反馈页聊天宽度绑定回归；已完成正式构建与现有自动化测试验证。
- 当前修改模块：反馈系统桌面端附件选择修复模块

## 已完成内容

- 已确认并修复桌面端附件选择“静默失败”问题：
  - `FeedbackWorkspace.qml`
  - `FeedbackViewModel.cpp`
  - `FeedbackViewModel.h`

- 已补齐附件选择失败提示链路：
  - QML 侧新增 `acceptSelectedAttachments()`
  - ViewModel 新增 `attachmentSelectionError`
  - 当未新增任何待发送附件时，界面会弹出明确错误提示，而不是无反应

- 已增强本地路径解析兼容性：
  - 兼容 `QVariantList`
  - 兼容 `QUrl`
  - 兼容 `file://` 字符串
  - 兼容本地路径字符串

- 已修复反馈页独立 QML 回归：

```text
FeedbackWorkspace.qml:300: TypeError: Cannot read property 'width' of null
```

- 已完成正式构建，安装包产物如下：

```text
G:\data\app\DIT-tools\output\v0.1.107\CineVault-Setup-v0.1.107.exe
Length: 167393964
LastWriteTime: 2026/7/1 13:37:17
```

- 已执行现有自动化测试：

```text
6/6 通过
100% tests passed, 0 tests failed out of 6
```

## 代码前后对比

### 1. 附件选择后无提示

修改前：

```qml
onAccepted: if (viewModel) viewModel.addAttachmentUrls(selectedFiles)
```

修改后：

```qml
onAccepted: root.acceptSelectedAttachments()
```

```qml
if (viewModel.attachmentSelectionError.length > 0) {
    attachmentSelectionDialog.text = viewModel.attachmentSelectionError
    attachmentSelectionDialog.open()
}
```

效果：

```text
以前：选了文件但没有任何提示
现在：要么出现待发送附件胶囊，要么弹出明确失败原因
```

### 2. ViewModel 静默跳过附件

修改前：

```cpp
Q_INVOKABLE void addAttachmentUrls(const QVariantList &urls);
```

```cpp
if (!info.exists() || !info.isFile()) {
    continue;
}
```

修改后：

```cpp
Q_PROPERTY(QString attachmentSelectionError READ attachmentSelectionError NOTIFY stateChanged)
Q_INVOKABLE void addAttachmentUrls(const QVariant &urls);
```

```cpp
if (addedCount == 0) {
    ...
    m_attachmentSelectionError = QStringLiteral("所选项目未能识别为本地文件，请重新选择本机文件。");
}
```

效果：

```text
以前：失败即静默吞掉
现在：失败原因会反馈到 UI
```

### 3. 聊天气泡宽度绑定空对象

修改前：

```qml
width: ListView.view.width
property real maxBubbleWidth: Math.min(parent.width * 0.78, 720)
```

修改后：

```qml
width: ListView.view ? ListView.view.width : 0
property real maxBubbleWidth: Math.max(120, Math.min(width * 0.78, 720))
```

## 待办清单（未完成）

- 在完整安装环境中做最终人工烟测：
  - 只选附件，确认胶囊立即出现
  - 文本 + 附件，确认发送成功
  - web 管理端确认收到桌面端附件
- 如需进入发布模块，可基于 `v0.1.107` 安装包继续验证或发布

## 下一步

- 下一步建议先安装 `output/v0.1.107/CineVault-Setup-v0.1.107.exe`，用完整部署环境完成最后一轮反馈页附件发送人工烟测，再决定是否进入发布或继续改进。
