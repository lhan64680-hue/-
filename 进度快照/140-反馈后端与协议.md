# 140-反馈后端与协议

日期：2026-07-01

## 已完成内容

- 已完成模块 5：反馈后端与协议。
- 已新增 `services/feedback/server` 首版后端：
  - `FastAPI`
  - `SQLite`
  - `WebSocket`
- 已完成客户端会话初始化、消息拉取、消息发送接口。
- 已完成开发者登录、会话列表、消息列表、回复、状态更新接口。
- 已完成附件上传与下载路径。
- 已完成在线实时推送与离线重连后历史补拉的协议底座。
- 已补齐客户端恢复长期会话需要的 `client_token` 返回字段。
- 已按约定加入单文件 `100MB` 默认限制。

## 当前修改到哪个模块

当前完成模块：005 反馈后端与协议。

## 具体修改的代码前后对比

### 修改前

```text
services/feedback/server/
  （空）
```

### 修改后

```python
@app.post("/api/client/session")
async def create_or_restore_client_session(request: Request, payload: ClientSessionPayload) -> JSONResponse:
    conversation, messages = database.get_or_create_client_conversation(request, payload)
    return JSONResponse({"conversation": conversation, "messages": messages})
```

```python
@app.post("/api/admin/conversations/{conversation_id}/status")
async def update_admin_status(
    request: Request,
    conversation_id: str,
    payload: AdminStatusPayload,
    authorization: str | None = Header(default=None),
) -> JSONResponse:
    verify_admin_token(parse_bearer_token(authorization))
    conversation = database.update_status(request, conversation_id, payload.status)
    await hub.broadcast_conversation(conversation)
    return JSONResponse({"conversation": conversation})
```

## 验证结果

- 本地 API smoke：通过
- 会话创建、带附件发消息、管理员回复、状态更新：通过
- 静态页面根路径 `/` 返回：通过

## 待办清单（未完成）

- 模块 6：开发者 Web 对话台
- 模块 7：桌面端“使用反馈”页面与实时提醒
- 模块 8：联调、构建、部署、清理

## 下一步要做什么

下一步继续完成开发者 Web 对话台，把聊天布局、登录、会话列表、回复和实时推送完整接到这套反馈后端上。
