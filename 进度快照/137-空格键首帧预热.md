# 137-空格键首帧预热

日期：2026-07-01

## 已完成内容

- 已完成模块 2：空格键首帧预热。
- 已在 `VideoPreviewPlayer.qml` 中新增隐藏态首帧预热链路：
  - 新增 `pendingFullscreenOpen`
  - 新增 `fullscreenPrimePauseTimer`
  - 预热期间静音
- 已调整全屏打开策略：
  - 无播放上下文时先预热第一帧，再显示全屏窗口
  - 有当前播放位置或当前停留帧时，继续沿用原同步逻辑

## 当前修改到哪个模块

当前完成模块：002 空格键首帧预热。

## 具体修改的代码前后对比

### 修改前

```qml
fullScreenPlayer.source = sourceUrl
fullScreenWindow.visibility = Window.FullScreen
Qt.callLater(primeFullscreenPlayback)
```

### 修改后

```qml
fullScreenPlayer.source = sourceUrl
if (pendingFullscreenOpen) {
    fullscreenPrimePauseTimer.stop()
    fullScreenPlayer.play()
    return
}
fullScreenWindow.visibility = Window.FullScreen
Qt.callLater(primeFullscreenPlayback)
```

## 验证结果

- 逻辑走查：通过
- 仍待完整 Qt 运行环境人工验证 `Space` 首开视频路径

## 待办清单（未完成）

- 模块 3：任务页总量进度与右侧详情
- 模块 4：设置页抽帧模式三态化
- 模块 5-7：反馈后端、开发者 Web、桌面反馈页
- 模块 8：构建、联调、部署、清理

## 下一步要做什么

下一步进入模块 3，给视频解析队列补批次汇总状态，并把任务页改成左侧总量进度 + 任务列表、右侧详情栏布局。
