# 进度快照 089

- 时间：2026-06-27
- 已完成内容：素材备份页“级联拷贝”默认改为关闭。
- 当前修改模块：素材备份默认设置模块。
- 阶段备份：
  - `backup/v0.1.74`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/domain/Entities.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/viewmodels/MinimalMaterialBackupViewModel.h`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/MaterialBackupWorkspace.qml`

## 已完成内容

- 将 `BackupRequest` 与 `BackupPlan` 的 `cascadeEnabled` 默认值从 `true` 改为 `false`。
- 将真实素材备份 ViewModel 的 `m_cascadeEnabled` 默认值从 `true` 改为 `false`。
- 将最小 GUI 占位 ViewModel 的 `m_cascadeEnabled` 默认值从 `true` 改为 `false`。
- 将 QML 中缺少 viewModel 时的 fallback 选中态从 `true` 改为 `false`。

## 当前修改到哪个模块

- 当前完成模块：默认不开启级联备份。
- 下一模块：素材备份执行中的进度微光动效。

## 代码前后对比

### 1. 备份实体默认值

修改前：

```cpp
bool cascadeEnabled = true;
```

修改后：

```cpp
bool cascadeEnabled = false;
```

### 2. ViewModel 默认值

修改前：

```cpp
bool m_cascadeEnabled = true;
```

修改后：

```cpp
bool m_cascadeEnabled = false;
```

### 3. QML fallback

修改前：

```qml
checked: viewModel ? viewModel.cascadeEnabled : true
```

修改后：

```qml
checked: viewModel ? viewModel.cascadeEnabled : false
```

## 待办清单（未完成）

- 为素材备份页执行备份时的整体进度条和任务进度条增加微光动效。
- 完成真实工作流构建、CTest、安装包打包、启动烟测和临时目录清理。
- 生成最终模块快照和备份对比文档。

## 下一步要做什么

继续修改 `MaterialBackupWorkspace.qml`，为运行态进度条增加微光流动效果。
