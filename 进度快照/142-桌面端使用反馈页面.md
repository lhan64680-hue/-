# 142-桌面端使用反馈页面

日期：2026-07-01

## 已完成内容

- 已完成模块 7：桌面端“使用反馈”页面。
- 已新增 `WorkspaceId::Feedback`，并把它接入顶栏工作区标签。
- 已完成 `FeedbackService`、`FeedbackViewModel`、`FeedbackMessageListModel` 和 `FeedbackWorkspace.qml`。
- 已完成首次填写昵称与联系方式、后续长期会话恢复。
- 已完成文本、图片、文件发送。
- 已完成开发者回复实时接收、离线后重开自动补收。
- 已完成顶栏未读提醒。
- 已在本机补齐 `Qt WebSockets` 组件，并通过正式 Windows 构建脚本生成新版安装包。

## 当前修改到哪个模块

当前完成模块：007 桌面端“使用反馈”页面。

## 具体修改的代码前后对比

### 修改前

```cpp
enum class WorkspaceId : qint32 {
    ProjectLibrary = 0,
    Import,
    Library,
    MaterialCenter,
    Qc,
    Report,
    Jobs
};
```

```qml
visible: root.shellViewModel.currentWorkspace === root.shellViewModel.jobsWorkspaceId
```

### 修改后

```cpp
enum class WorkspaceId : qint32 {
    ProjectLibrary = 0,
    Import,
    Library,
    MaterialCenter,
    Qc,
    Report,
    Jobs,
    Feedback
};
```

```qml
visible: root.shellViewModel.currentWorkspace === root.shellViewModel.feedbackWorkspaceId
```

```cpp
Q_PROPERTY(int feedbackWorkspaceId READ feedbackWorkspaceId CONSTANT)
```

## 验证结果

- 正式 Windows 构建脚本：通过
- 新安装包产物输出：通过
- 桌面端反馈页运行态人工联调：待模块 8 统一验收

## 待办清单（未完成）

- 模块 8：反馈系统远端部署、Nginx/PM2 配置、最终联调与清理

## 下一步要做什么

下一步进入模块 8，把反馈后端和管理端部署到服务器 `/www/APP/DIT-TOOLS`，配置 PM2 与 Nginx 路由 `/dit-feedback/`，然后做远端 smoke 和最终清理。
