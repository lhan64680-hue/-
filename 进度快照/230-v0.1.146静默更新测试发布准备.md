# v0.1.146 静默更新测试发布准备

## 任务目标

发布正式稳定版 `v0.1.146`，供已安装的 `v0.1.145` 客户端检测更新，验证独立可视化更新窗口、安装包下载、UAC 提权、静默安装和自动重启的完整链路。

## 本次变更范围

- 不修改功能代码，继续使用 `v0.1.145` 已完成并通过测试的独立更新器实现。
- 构建时将运行时版本注入为 `0.1.146`，生成新的 Windows 安装包。
- 发布为 GitHub 正式稳定版，确保客户端的 `releases/latest` 接口能够发现该版本。

## 发布步骤

1. 确认工作区干净、`main` 与远端同步，并确认 `v0.1.146` 尚未占用。
2. 运行现有自动化测试。
3. 使用真实工作流构建 `CineVault-Setup-v0.1.146.exe`。
4. 校验安装包版本、大小和 SHA-256。
5. 创建并推送 `v0.1.146` 标签，发布正式 GitHub Release。
6. 核验 GitHub `latest` 已指向 `v0.1.146`，并记录远端资产摘要。

## 验收标准

- 自动化测试全部通过。
- 安装包文件版本为 `0.1.146`。
- GitHub 最新正式版本为 `v0.1.146`，安装包可公开下载。
- 远端资产大小和摘要与本地安装包一致。
- `v0.1.145` 能检测到新版本，用户可继续进行静默更新实机测试。

## 待办清单

- [ ] 运行自动化测试
- [ ] 构建并校验安装包
- [ ] 推送标签并创建正式 Release
- [ ] 核验远端最新版本与资产
- [ ] 写入最终发布快照和备份记录
