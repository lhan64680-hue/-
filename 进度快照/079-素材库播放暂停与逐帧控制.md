# 进度快照 079

- 时间：2026-06-27
- 已完成内容：修复素材库全屏播放暂停后显示固定缩略图的问题，并支持视频播放/已有预览帧时按左右键逐帧进退。
- 当前修改模块：模块 10，素材库播放暂停与逐帧控制。
- 阶段备份：
  - `backup/v0.1.64`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/ui/qml/components/VideoPreviewPlayer.qml`
  - `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/LibraryWorkspace.qml`
  - `docs/素材库与素材管理体验优化方案.md`

## 已完成内容

- `VideoPreviewPlayer.qml` 增加“已有视频帧”状态，播放、暂停、拖动和逐帧后不再让缩略图海报盖住视频输出。
- 全屏播放器暂停时将 `fullscreenFrameRequested` 标记为 true，避免暂停状态回到固定缩略图。
- 全屏播放器支持 `←` / `→` 单次按键逐帧后退/前进，并在逐帧时自动暂停。
- 素材库页面左右键路由改为：视频正在播放或已有预览帧时优先逐帧；否则保持原来的素材选择。
- 重新打包生成 `output/v0.1.65/CineVault-Setup-v0.1.65.exe`。

## 代码前后对比

### 1. 缩略图海报显示条件

修改前：

```qml
readonly property bool showThumbnailPoster: hasThumbnail && !previewPlaybackRequested && !previewFrameRequested
```

修改后：

```qml
readonly property bool hasPreviewFrame: previewPlaybackRequested
    || previewFrameRequested
    || mediaPlayer.playbackState === MediaPlayer.PlayingState
    || mediaPlayer.position > 0
readonly property bool hasFullscreenFrame: fullscreenFrameRequested
    || fullScreenPlayer.playbackState === MediaPlayer.PlayingState
    || fullScreenPlayer.position > 0
readonly property bool showThumbnailPoster: hasThumbnail && !hasPreviewFrame
readonly property bool showFullscreenPoster: hasThumbnail && !hasFullscreenFrame
```

### 2. 全屏暂停不再显示缩略图

修改前：

```qml
if (isFullscreenPlaying) {
    fullScreenPlayer.pause()
} else {
    fullScreenPlayer.play()
}
```

修改后：

```qml
if (isFullscreenPlaying) {
    fullScreenPlayer.pause()
    fullscreenFrameRequested = true
} else {
    fullscreenFrameRequested = true
    fullScreenPlayer.play()
}
```

### 3. 左右键逐帧

修改前：

```qml
Image {
    anchors.fill: parent
    source: videoPreview.thumbnailUrl
    visible: videoPreview.hasThumbnail
             && fullScreenPlayer.playbackState !== MediaPlayer.PlayingState
}
```

修改后：

```qml
function stepFullscreenFrame(direction) {
    if (!hasSource || fullScreenPlayer.duration <= 0) {
        return false
    }

    var targetPosition = Math.max(0, Math.min(fullScreenPlayer.position + direction * frameDurationMs(fullScreenPlayer), fullScreenPlayer.duration))
    fullscreenFrameRequested = true
    fullScreenPlayer.position = targetPosition
    fullScreenPlayer.pause()
    return true
}

Keys.onPressed: function(event) {
    if (event.isAutoRepeat) {
        return
    }

    if (event.key === Qt.Key_Left) {
        event.accepted = videoPreview.stepFullscreenFrame(-1)
    } else if (event.key === Qt.Key_Right) {
        event.accepted = videoPreview.stepFullscreenFrame(1)
    }
}

Image {
    anchors.fill: parent
    source: videoPreview.thumbnailUrl
    visible: videoPreview.showFullscreenPoster
}
```

### 4. 素材库左右键路由

修改前：

```qml
if (event.key === Qt.Key_Left) {
    viewModel.moveAssetSelection(-1)
    event.accepted = true
} else if (event.key === Qt.Key_Right) {
    viewModel.moveAssetSelection(1)
    event.accepted = true
}
```

修改后：

```qml
function stepSelectedPreviewFrame(direction) {
    if (!viewModel || !viewModel.selectedPreviewIsVideo || !fullscreenPlaybackBridge.frameNavigationActive) {
        return false
    }

    return fullscreenPlaybackBridge.stepFrameFromKeyboard(direction)
}

if (event.key === Qt.Key_Left) {
    if (libraryRoot.stepSelectedPreviewFrame(-1)) {
        event.accepted = true
        return
    }
    viewModel.moveAssetSelection(-1)
    event.accepted = true
} else if (event.key === Qt.Key_Right) {
    if (libraryRoot.stepSelectedPreviewFrame(1)) {
        event.accepted = true
        return
    }
    viewModel.moveAssetSelection(1)
    event.accepted = true
}
```

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.64`。
- 最小 GUI 构建：`cmake --build --preset windows-msvc-release --config Release`，成功。
- 真实工作流构建：`cmake --build --preset windows-msvc-release-real --config Release`，成功。
- 测试命令：
  - `ctest --test-dir build/windows-msvc-release --output-on-failure`
  - `ctest --test-dir build/windows-msvc-release-real --output-on-failure`
- 测试结果：两次命令均成功执行，当前构建目录提示 `No tests were found!!!`。
- 重新打包命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`。
- 新安装包：`output/v0.1.65/CineVault-Setup-v0.1.65.exe`。
- 安装包大小：`55443359` 字节。
- 安装包 SHA256：`916C0DFAE3F63E269AF75D73017439BF943B6E391B3177785387C1C98FC71078`。
- 临时目录：`output/_installer_staging` 为空。

## 待办清单（未完成）

- 使用真实视频素材人工复核：空格播放后再次空格暂停，画面是否停留在当前播放帧。
- 使用真实视频素材人工复核：播放中按 `←` / `→` 是否按预期单帧进退。
- 若需要严格按素材真实 fps 精确逐帧，可后续把 ffprobe 帧率从 C++ ViewModel 暴露给 QML，替换播放器元数据/40ms 回退策略。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材库与素材管理体验优化方案.md`。若用户反馈逐帧幅度与素材真实帧率不一致，下一步优先在素材查询模型中暴露视频帧率字段，再让 `VideoPreviewPlayer.qml` 使用该字段计算单帧步长。
