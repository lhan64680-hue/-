# 132-升级本地安装版到v0.1.98并完成启动烟测

日期：2026-06-30

## 已完成内容

- 已读取上一轮最新快照：`131-修复素材管理中心解析任务空错误写入.md`。
- 已核实当前用户实际运行的是本地安装版，不是源码构建目录：
  - 安装路径：`D:\Program Files\影资管家\`
  - 原安装版本：`v0.1.97`
  - 项目库：`D:\data\影视管家\002\project.cvdb`
  - 素材管理全局库：`D:\Program Files\影资管家\data\material-center.sqlite`
- 已核实库职责：
  - `002/project.cvdb` 中不存在 `video_analysis_task`
  - `video_analysis_task` 表位于安装版全局库 `material-center.sqlite`
  - `002` 项目的 148 条视频素材已同步进入全局库
- 已完成新安装包构建：
  - 安装包：`G:\data\app\DIT-tools\output\v0.1.98\CineVault-Setup-v0.1.98.exe`
  - SHA256：`AD071C30EE4BA581D5EDA7364053FBD829BABBA128E99278FC137038967C592B`
- 已静默升级本机安装版到 `v0.1.98`
- 已完成安装后启动烟测：
  - `D:\Program Files\影资管家\CineVault.exe` 可启动
  - 进程已成功拉起后再关闭

## 当前修改到哪个模块

当前完成模块：004 本地安装版升级与启动验证模块。

本轮没有新增业务代码修改，重点是把上一轮已经修好的解析状态持久化逻辑真正交付到用户当前正在运行的安装版中。

## 具体修改的代码前后对比

### 本轮源码变更

本轮无新增源码改动，沿用上一轮 `131` 中已完成的修复代码：

```cpp
const auto normalizedLastErrorMessage = task.lastErrorMessage.isNull()
    ? QStringLiteral("")
    : task.lastErrorMessage;
const auto lastUpdatedAt = task.lastUpdatedAt.isEmpty() ? nowIso() : task.lastUpdatedAt;

query.addBindValue(task.summaryRetryCount);
query.addBindValue(normalizedLastErrorMessage);
query.addBindValue(lastUpdatedAt);
```

### 本轮产物变化

```text
安装版从 v0.1.97 升级到 v0.1.98
安装路径仍为 D:\Program Files\影资管家\
全局素材库 material-center.sqlite 未被安装流程覆盖
```

## 验证结果

- `build_windows.ps1 -RealWorkflow`：通过
- `CineVault-Setup-v0.1.98.exe` 构建：通过
- 本机静默安装升级：通过
- 卸载注册表版本：
  - `DisplayName = 影资管家 v0.1.98`
  - `DisplayVersion = 0.1.98`
- 安装后可执行文件时间戳：
  - `D:\Program Files\影资管家\CineVault.exe`
  - `2026-06-30 14:44:44`
- 启动烟测：通过

## 待办清单（未完成）

- 在 `v0.1.98` 安装版内复测素材管理中心整段解析主路径
- 在 `v0.1.98` 安装版内复测单帧重解析路径
- 如仍有异常，继续抓安装版运行时日志和全局库写入状态

## 下一步要做什么

下一步直接基于当前本机安装版 `v0.1.98` 复测素材管理中心解析。如果再次报错，下一轮从这份 `132` 快照继续，优先抓运行时现场，不重复回溯已完成的版本升级工作。
