# 199-全字段搜索与筛选

- 时间：2026-07-04
- 当前模块：模块 3，全字段搜索与筛选

## 已完成内容

- 已读取上一份快照：`进度快照/198-图片与文本解析能力.md`。
- 已补充本模块代码对比文档：`backup/v0.1.154/全字段搜索与筛选-代码对比.md`。
- 查询服务新增素材类型筛选接口。
- 搜索条件扩展到文件名、绝对路径、相对路径、扩展名、项目名、素材源、技术摘要、AI 摘要、关键词、视频逐帧描述和文本正文。
- ViewModel 接入素材类型筛选数据和 `setAssetTypeFilter`。
- 状态筛选新增“仅索引”。
- 已检查 FTS 写入点，旧 8 列写入已改为新 13 列写入。

## 当前修改到哪个模块

- `dit-tools-src/cinevault-pro/src/application/MaterialCenterQueryService.h`
- `dit-tools-src/cinevault-pro/src/application/MaterialCenterQueryService.cpp`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.h`
- `dit-tools-src/cinevault-pro/src/ui/viewmodels/MaterialCenterViewModel.cpp`
- `dit-tools-src/cinevault-pro/src/application/VideoAnalysisService.cpp`

## 具体修改的代码前后对比

见 `backup/v0.1.154/全字段搜索与筛选-代码对比.md`。

## 验证结果

- `git diff --check -- <模块3相关文件>`：通过，仅有既有 LF/CRLF 提示。

## 待办清单（未完成）

- 模块 4：UI 和模型从视频视角改为素材视角。
- 模块 5：补测试、构建、打包、清理。

## 下一步要做什么

- 进入模块 4，调整素材管理中心列表、详情栏、类型筛选和按钮状态。
