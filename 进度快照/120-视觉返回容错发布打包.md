# 120-视觉返回容错发布打包

日期：2026-06-29

## 已完成内容

- 已读取上一轮最新快照：`119-视觉返回纯文本兜底落库.md`。
- 已读取 GitHub 发布相关技能说明，并确认本地 `gh` 已登录。
- 已同步远端 tags，确认当前最新 GitHub Release 为 `v0.1.92`。
- 已创建阶段备份：`backup/v0.1.102`。
- 已执行最终打包验证模块，生成新版安装包 `v0.1.94`。
- 已完成全量单测、部署形态 5 秒启动烟测、安装包哈希校验和临时缓存清理。
- 已新增模块溯源文档：`backup/v0.1.102/视觉返回容错发布打包-前后对比.md`。

## 当前修改到哪个模块

当前完成模块：最终打包验证模块。

视觉返回格式容错链路已完成并进入可发布状态：

1. 标准 JSON 解析。
2. 字段别名和字符串/数组归一化。
3. 一次自动格式修复请求。
4. 自动修复失败后的纯文本兜底落库。
5. 新版安装包构建和验证。

## 具体修改的代码前后对比

本模块不新增业务代码，主要完成构建产物更新与发布前验证。

### 打包前

```text
- 最新已发布 GitHub Release：v0.1.92
- 本地最新安装包目录：output/v0.1.93
- 本轮视觉返回容错代码尚未生成新版安装包
```

### 打包后

```text
- 已生成 output/v0.1.94/CineVault-Setup-v0.1.94.exe
- 安装包大小：57,797,137 字节
- SHA256：B55DBC514856317164F9FDF54D8E987F6EB6EAB89829DC54EAC820A8E7348C22
```

## 验证结果

- 真实工作流打包：通过。
  - `powershell -NoProfile -ExecutionPolicy Bypass -File .\tool\build_windows.ps1 -RealWorkflow`
- 全量单测：通过，6/6。
  - `ctest --test-dir build/windows-msvc-release-real --output-on-failure`
- 部署形态 5 秒启动烟测：通过。
  - 临时部署目录启动后 5 秒内保持运行，随后手动结束进程。
- 临时缓存清理：完成。
  - 已清理空的 `output/_installer_staging`
  - 已清理临时烟测目录 `output/_smoke_v0.1.94`

## 待办清单（未完成）

- 提交当前变更。
- 推送到 GitHub。
- 创建 GitHub Release `v0.1.94` 并上传安装包。
- 使用真实视觉接口复核 CSV、纯文本、Markdown 表格返回时的素材管理中心解析表现。

## 下一步要做什么

提交、推送到 `origin/main`，并创建 GitHub Release `v0.1.94`。
