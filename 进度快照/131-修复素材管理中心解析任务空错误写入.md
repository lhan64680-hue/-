# 131-修复素材管理中心解析任务空错误写入

日期：2026-06-30

## 已完成内容

- 已读取上一轮最新快照：`130-github发布v0.1.97完成.md`。
- 已定位素材管理中心解析报错根因：
  - `video_analysis_task.last_error_message` 字段为 `NOT NULL`
  - 任务状态保存时，`QString()`/`clear()` 形成的 null 字符串被绑定进 SQL，触发约束失败
- 已完成最小范围修复：
  - 在 `persistAnalysisTask()` 内统一把 `lastErrorMessage` 的 null 字符串归一化为 `''`
  - 同时提取 `lastUpdatedAt` 绑定值，保持写入逻辑一致
- 已完成源码阶段备份：
  - 备份目录：`G:\data\app\DIT-tools\backup\v0.1.110`
  - 差异文档：`素材管理中心解析错误-前后对比.md`
- 已完成真实工作流编译验证：
  - `cmake --preset windows-msvc-release-real`
  - `cmake --build --preset windows-msvc-release-real --config Release`

## 当前修改到哪个模块

当前完成模块：003 素材管理中心解析任务状态持久化修复模块。

本轮只处理“素材管理中心解析时报 `NOT NULL constraint failed: video_analysis_task.last_error_message`”这一处问题，没有扩散修改到素材查询、表结构迁移或 UI 展示层。

## 具体修改的代码前后对比

### 修改前

```cpp
query.addBindValue(task.summaryRetryCount);
query.addBindValue(task.lastErrorMessage);
query.addBindValue(task.lastUpdatedAt.isEmpty() ? nowIso() : task.lastUpdatedAt);
```

### 修改后

```cpp
const auto normalizedLastErrorMessage = task.lastErrorMessage.isNull()
    ? QStringLiteral("")
    : task.lastErrorMessage;
const auto lastUpdatedAt = task.lastUpdatedAt.isEmpty() ? nowIso() : task.lastUpdatedAt;

query.addBindValue(task.summaryRetryCount);
query.addBindValue(normalizedLastErrorMessage);
query.addBindValue(lastUpdatedAt);
```

## 验证结果

- 真实工作流 CMake 配置：通过
- 真实工作流增量编译：通过
- 受影响文件：
  - `G:\data\app\DIT-tools\dit-tools-src\cinevault-pro\src\application\VideoAnalysisService.cpp`

## 待办清单（未完成）

- 在应用内复测素材管理中心的整段视频解析主路径
- 复测单帧重解析路径，确认成功分支不会再写入 `NULL`
- 如需对外安装包验证，再执行一次完整打包流程

## 下一步要做什么

下一步直接在应用里复测素材管理中心解析。如果还有新报错，下一轮从这份 `131` 快照继续，只继续处理解析链路，不回头重复已完成内容。
