# 进度快照 078

- 时间：2026-06-27
- 已完成内容：根据安装错误截图继续定位，发现本机仍残留正式安装目录下的 `CineVault.exe` 进程；已结束残留进程，并将安装器清理旧进程的时机提前到进入安装前。
- 当前修改模块：模块 9，安装器提前清理残留进程与重新打包。
- 阶段备份：
  - `backup/v0.1.63`
- 修改文件：
  - `installer/windows/cinevault.iss`
  - `docs/素材库与素材管理体验优化方案.md`

## 已完成内容

- 用户反馈安装器显示 Inno 默认错误页：`Setup was unable to automatically close all applications`。
- 本机检测到残留进程：

```text
PID: 5628
路径: D:\Program Files\影资管家\CineVault.exe
启动时间: 2026/6/26 19:53:49
```

- 已执行 `Stop-Process -Id 5628 -Force`，复查结果为 `CINEVAULT_PROCESS_CLOSED`。
- 安装器补充 `NextButtonClick(wpReady)` 钩子，在用户从准备安装页继续前先执行旧进程清理。
- 重新打包生成 `output/v0.1.64/CineVault-Setup-v0.1.64.exe`。

## 代码前后对比

### 安装前清理时机

修改前：

```iss
function PrepareToInstall(var NeedsRestart: Boolean): String;
begin
  Result := '';
  if not StopCineVaultProcesses('file installation') then
    Result := '旧版影资管家进程仍在运行，安装已取消。';
end;

function InitializeUninstall(): Boolean;
begin
  Result := StopCineVaultProcesses('uninstall initialization');
end;
```

修改后：

```iss
function PrepareToInstall(var NeedsRestart: Boolean): String;
begin
  Result := '';
  if not StopCineVaultProcesses('file installation') then
    Result := '旧版影资管家进程仍在运行，安装已取消。';
end;

function NextButtonClick(CurPageID: Integer): Boolean;
begin
  Result := True;

  if CurPageID = wpReady then
    Result := StopCineVaultProcesses('ready page');
end;

function InitializeUninstall(): Boolean;
begin
  Result := StopCineVaultProcesses('uninstall initialization');
end;
```

说明：上一版只在初始化和 `PrepareToInstall` 阶段清理旧进程；新版在正常向导流程的 `wpReady` 下一步点击前也会清理，避免 Inno 自身文件占用检测前旧进程仍存活。

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.63`。
- 残留进程处理：`Stop-Process -Id 5628 -Force` 后，`Get-Process -Name CineVault` 返回空。
- 重新打包命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`。
- 构建结果：C++ 构建为最新状态，Inno Setup `[Code]` 段编译成功。
- 新安装包：`output/v0.1.64/CineVault-Setup-v0.1.64.exe`。
- 安装包大小：`55439472` 字节。
- 安装包 SHA256：`7749CB357FB6CA624609CC88BCE44B4B0E5850478B04E12119778C64972E086A`。
- 测试命令：`ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure`。
- 测试结果：命令成功执行，当前构建目录提示 `No tests were found!!!`。
- 临时目录：`output/_installer_staging` 无残留版本目录。
- 当前进程检查：`CineVault.exe` 进程数为 0。

## 待办清单（未完成）

- 使用 `output/v0.1.64/CineVault-Setup-v0.1.64.exe` 人工复核覆盖安装流程。
- 如仍出现 Inno 默认关闭应用错误页，优先排查是否运行了旧安装包，或是否有非 `CineVault.exe` 进程占用安装目录文件。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材库与素材管理体验优化方案.md`。若用户仍反馈安装占用错误，下一步应读取安装日志，定位 Inno 报告的具体占用文件或进程，再考虑增加按安装目录路径清理所有子进程的逻辑。
