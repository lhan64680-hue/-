# 129-修复旧库启动阶段索引迁移

日期：2026-06-30

## 已完成内容

- 已读取上一轮最新快照：`128-github发布v0.1.96完成.md`。
- 已创建新的阶段备份：`backup/v0.1.109`。
- 已继续核对 002 项目真实数据：
  - `project_registry` 中存在 002
  - `global_video_asset` 中存在 `148` 条 002 项目素材
- 已确认“素材管理中心仍为空”的第二层根因：
  - 不是同步问题
  - 不是查询结果为空
  - 是 `createSchema()` 阶段仍提前创建了依赖 `analysis_state` 的索引
- 已修复 `GlobalDatabaseManager.cpp`：
  - 从 `createSchema()` 中移除 `idx_frame_video_state`
  - 保持该索引只在兼容迁移最后创建
- 已用旧库副本按完整启动顺序验证：
  - `createSchema -> ensureSchemaCompatibility`
  - 迁移通过，字段可补齐
- 已完成构建验证：
  - `cmake --build G:/data/app/DIT-tools/dit-tools-src/cinevault-pro/build/windows-msvc-release-real --config Release`
- 已完成测试验证：
  - `ctest --test-dir G:/data/app/DIT-tools/dit-tools-src/cinevault-pro/build/windows-msvc-release-real -C Release --output-on-failure`
  - `6/6` 通过
- 已构建修复版安装包：
  - `output/v0.1.97/CineVault-Setup-v0.1.97.exe`
  - 大小：`57,817,064` 字节
  - SHA256：`70BE7E1254E920A992D2C540DADB7D1D1B6BCAA450A22AC4271E492B9D949E75`

## 当前修改到哪个模块

当前完成模块：旧库启动阶段迁移修复模块。

这次已经把“兼容迁移代码正确，但还没执行到兼容迁移之前就先炸掉”的链路修平，下一步只剩 GitHub 发布。

## 具体修改的代码前后对比

### 修改前

```text
- createSchema() 先创建依赖 analysis_state 的索引
- 旧库缺字段时直接在启动前半段失败
- 002 项目素材虽然在库里，但素材管理中心查询层根本起不来
```

### 修改后

```text
- createSchema() 不再提前建该索引
- 索引统一挪到 ensureSchemaCompatibility() 最后创建
- 旧库可以完整走完启动迁移路径
```

## 验证结果

- 002 项目真实素材数：`148`
- 旧库副本完整启动迁移模拟：通过
- `cmake --build ... --config Release`：通过
- `ctest ... -C Release --output-on-failure`：通过
- 修复版安装包已生成：`v0.1.97`

## 待办清单（未完成）

- 提交修复代码与快照
- 推送 `origin/main`
- 创建并推送 `v0.1.97`
- 创建 GitHub Release `v0.1.97`
- 让用户更新测试

## 下一步要做什么

继续完成 GitHub 发布，把 `v0.1.97` 推给你直接测试。
