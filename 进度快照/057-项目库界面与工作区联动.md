# 进度快照 057

- 时间：2026-06-26 16:26:51 +08:00
- 已完成内容：新增「项目库」工作区入口和项目卡片页面，完成顶部标签顺序、全局搜索分流、点击项目卡片进入素材库、项目库页隐藏侧栏/检查器/底部任务栏、素材库未打开项目引导态。
- 当前修改模块：项目库 UI、导航与工作区联动模块，涉及 `Enums.h`、`Formatters.cpp`、`ShellViewModel.h/.cpp`、`MinimalShellViewModel.h/.cpp`、`AppContext.h/.cpp`、`Main.qml`、`TopCommandBar.qml`、`LibraryWorkspace.qml`、`ProjectLibraryWorkspace.qml`、`CMakeLists.txt`。
- 阶段备份：沿用本阶段开始前创建的 `backup/v0.1.48`。
- 代码前后对比：
  - 修改前：顶部标签顺序从「素材库」开始，启动默认停留素材库，没有项目库工作区。
  - 修改后：新增 `WorkspaceId::ProjectLibrary`，启动默认进入「项目库」，标签顺序为「项目库」「素材库」「素材管理中心」「报表」「任务」。
  - 修改前：主布局只在素材管理中心隐藏素材源栏和检查器，未打开项目时进入素材库会显示空白内容区。
  - 修改后：项目库页隐藏素材源栏、检查器和底部任务栏；未打开项目进入素材库时显示“请先选择项目”的引导态。
  - 修改前：全局搜索只在素材库和素材管理中心之间分流。
  - 修改后：当前工作区为项目库时搜索项目，其他素材相关页面保持原有素材搜索逻辑。
  - 修改前：没有项目卡片点击进入素材库的流程。
  - 修改后：`ProjectLibraryWorkspace` 展示项目卡片，点击可打开项目并由 `AppContext` 切换到素材库，右键可从项目库移除入口。
- 待办清单（未完成）：
  - 实现 Windows 原生标题栏主题适配。
  - 补充本阶段前后对比文档最终内容。
  - 构建真实流程安装包、运行验证并清理临时缓存。
- 下一步要做什么：新增标题栏主题适配助手，并由 QML 在主题变化时调用。

