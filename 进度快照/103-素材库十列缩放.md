# 103-素材库十列缩放

日期：2026-06-27

## 已完成内容

- 已在新阶段开始前执行本地备份：`backup/v0.1.86`。
- 已仅修改素材库宫格缩放逻辑，未改动表格模式、卡片结构、选择行为、右键菜单、拖拽浏览和全屏预览。
- 已将缩略图最小缩放下限从 `0.85` 调整为 `0.5`，并统一提取为 `minThumbnailScale` / `maxThumbnailScale`。
- 已同步调整 Ctrl+滚轮缩放夹取范围与右下角滑杆范围。

## 当前修改到哪个模块

当前完成到模块 2：素材库十列缩放。

修改文件：
- `dit-tools-src/cinevault-pro/src/ui/qml/workspaces/LibraryWorkspace.qml`

## 具体修改的代码前后对比

### 最小缩放范围

修改前：

```qml
function adjustThumbnailScale(delta) {
    var nextScale = Math.max(0.85, Math.min(1.45, thumbnailScale + delta))
    thumbnailScale = Math.round(nextScale * 100) / 100
}
```

修改后：

```qml
readonly property real minThumbnailScale: 0.5
readonly property real maxThumbnailScale: 1.45

function adjustThumbnailScale(delta) {
    var nextScale = Math.max(minThumbnailScale, Math.min(maxThumbnailScale, thumbnailScale + delta))
    thumbnailScale = Math.round(nextScale * 100) / 100
}
```

### 滑杆范围

修改前：

```qml
ThemedSlider {
    from: 0.85
    to: 1.45
}
```

修改后：

```qml
ThemedSlider {
    from: libraryRoot.minThumbnailScale
    to: libraryRoot.maxThumbnailScale
}
```

## 验证结果

- 快速尺寸核算结果：`scale=0.5 card=112 cell=128 content=1320 columns=10`。
- 以上核算按 `1920` 主窗口宽度、源素材栏 `270`、右侧详情栏 `330` 的当前布局计算，最小缩放时内容区可达到单排 `10` 卡片。

## 待办清单（未完成）

- 实现 GitHub Release 自动检查、下载和安装调度。
- 在设置弹窗顶部增加“检查更新”按钮并接入同一流程。
- 实现启动自动检查更新、下载完成确认、退出安装和自动重启。
- 完成更新相关单测、真实工作流构建、安装包输出与发布说明。

## 下一步要做什么

进入模块 3，新增 GitHub 更新基础设施：
- 统一应用版本来源。
- 查询公开 GitHub 最新 Release。
- 选择安装包资产并下载到本地更新缓存。
- 记录待安装状态并为后续安装闭环提供入口。
