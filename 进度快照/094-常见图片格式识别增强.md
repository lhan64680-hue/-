# 094-常见图片格式识别增强

日期：2026-06-27

## 已完成内容

- 已按续写规则读取最新快照：`093-图片缩略图显示修复.md`。
- 已在模块开始前执行源码备份，生成 `backup/v0.1.78`。
- 已完成模块一：图片格式识别与缩略图链路增强。
- 已扩展图片类型识别范围，让更多常见图片格式进入 `AssetType::Image`：
  - 常规位图：`jpg/jpeg/jpe/png/apng/bmp/dib/gif/webp`
  - 专业图像：`tif/tiff/psd/psb/exr/hdr/tga`
  - 新格式：`heic/heif/avif/jxl`
  - JPEG 2000：`jp2/j2k/jpf/jpx/jpm`
  - 矢量/图标：`svg/svgz/ico`
  - 相机 RAW：`dng/raw/arw/cr2/cr3/nef/nrw/raf/rw2/orf/pef/srw/x3f`
- 已新增模块溯源文档：`backup/v0.1.78/常见图片格式识别增强-前后对比.md`。

## 当前修改到哪个模块

当前模块：图片格式识别模块。

修改文件：

- `dit-tools-src/cinevault-pro/src/core/scan/FileTypeService.cpp`

## 具体修改的代码前后对比

修改前：

```cpp
static const QSet<QString> imageExt = {QStringLiteral("jpg"), QStringLiteral("jpeg"), QStringLiteral("png"), QStringLiteral("dng"), QStringLiteral("tif"), QStringLiteral("tiff"), QStringLiteral("bmp")};
```

修改后：

```cpp
static const QSet<QString> imageExt = {
    QStringLiteral("jpg"), QStringLiteral("jpeg"), QStringLiteral("jpe"),
    QStringLiteral("png"), QStringLiteral("apng"),
    QStringLiteral("webp"), QStringLiteral("gif"),
    QStringLiteral("tif"), QStringLiteral("tiff"),
    QStringLiteral("psd"), QStringLiteral("psb"),
    QStringLiteral("bmp"), QStringLiteral("dib"),
    QStringLiteral("heic"), QStringLiteral("heif"), QStringLiteral("avif"),
    QStringLiteral("jp2"), QStringLiteral("j2k"), QStringLiteral("jpf"), QStringLiteral("jpx"), QStringLiteral("jpm"),
    QStringLiteral("jxl"), QStringLiteral("exr"), QStringLiteral("hdr"), QStringLiteral("pic"),
    QStringLiteral("tga"), QStringLiteral("icb"), QStringLiteral("vda"), QStringLiteral("vst"),
    QStringLiteral("svg"), QStringLiteral("svgz"), QStringLiteral("ico"),
    QStringLiteral("dng"), QStringLiteral("raw"), QStringLiteral("arw"), QStringLiteral("cr2"), QStringLiteral("cr3"),
    QStringLiteral("nef"), QStringLiteral("nrw"), QStringLiteral("raf"), QStringLiteral("rw2"), QStringLiteral("orf"),
    QStringLiteral("pef"), QStringLiteral("srw"), QStringLiteral("x3f")
};
```

## 验证结果

- 真实工作流 Release 增量构建通过。
- 真实工作流 `ctest` 通过：3/3。
- 启动烟测通过，进程已正常拉起并关闭。

## 待办清单（未完成）

- 模块二：给非图片/非视频文件增加精美类型图标展示。
- 模块二完成后重新执行真实 Release 打包，更新安装包产物。
- 人工用 `webp/tiff/psd` 样本重新导入，确认图片分类、计数和缩略图任务正常。

## 下一步要做什么

继续模块二，修改素材卡片 UI：当缩略图不可用时，根据素材类型和扩展名显示有层次的文件类型图标，而不是单纯渐变占位。
