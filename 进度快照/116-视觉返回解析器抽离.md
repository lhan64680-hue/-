# 116-视觉返回解析器抽离

日期：2026-06-29

## 已完成内容

- 已读取上一轮最新快照：`115-推送webp缩略图修复到main.md`。
- 已确认当前工作区修改前为干净状态。
- 已创建阶段备份：`backup/v0.1.98`。
- 已完成模块一：视觉返回解析器抽离。
- 新增 `VisionResponseParser.h/.cpp`，承接原来 `VisionApiClient.cpp` 内部的 assistant 返回内容提取、JSON 对象抽取和 JSON 解析职责。
- `VisionApiClient.cpp` 改为只负责接口请求、请求降级和业务字段转换，三个解析入口统一调用 `VisionResponseParser::parseAssistantJson()`。
- `VisionApiClientJsonTest` 不再直接包含 `VisionApiClient.cpp`，改为测试独立解析器模块。
- 已新增模块溯源文档：`backup/v0.1.98/视觉返回解析器抽离-前后对比.md`。

## 当前修改到哪个模块

当前完成模块：模块一，视觉返回解析器抽离。

本模块只做职责拆分，不改变现有 JSON 容错行为；CSV、纯文本和字段归一化策略留到后续模块逐步实现。

## 具体修改的代码前后对比

### 解析职责抽离

修改前：

```cpp
// VisionApiClient.cpp
QString extractMessageContent(const QJsonValue &contentValue);
QString extractJsonBlock(QString text);
std::optional<QJsonObject> parseAssistantJson(const QByteArray &responseBody, QString *errorMessage);
```

修改后：

```cpp
// VisionResponseParser.h
namespace VisionResponseParser {

std::optional<QJsonObject> parseAssistantJson(const QByteArray &responseBody, QString *errorMessage);

}
```

### 视觉客户端调用解析器

修改前：

```cpp
auto payload = parseAssistantJson(result.body, errorMessage);
```

修改后：

```cpp
auto payload = VisionResponseParser::parseAssistantJson(result.body, errorMessage);
```

### 单测边界

修改前：

```cpp
#include "infrastructure/network/VisionApiClient.cpp"
```

修改后：

```cpp
#include "infrastructure/network/VisionResponseParser.h"

using VisionResponseParser::parseAssistantJson;
```

## 验证结果

- 相关测试目标构建：通过。
  - `cmake --build build/windows-msvc-release-real --target VisionApiClientJsonTest --config Release`
- 视觉 JSON 容错单测：通过，1/1。
  - `ctest --test-dir build/windows-msvc-release-real -R VisionApiClientJsonTest --output-on-failure`
- 主程序构建：通过。
  - `cmake --build build/windows-msvc-release-real --target CineVault --config Release`
- 全量单测：通过，6/6。
  - `ctest --test-dir build/windows-msvc-release-real --output-on-failure`

## 待办清单（未完成）

- 模块二：新增字段归一化与轻量 schema 校验，兼容字段别名、数组字符串混用和轻微字段缺失。
- 模块三：增加一次自动修复请求，把 CSV、纯文本、Markdown 表格或半结构文本拉回目标 JSON。
- 模块四：修复失败后增加纯文本兜底落库，避免格式问题导致视频整体失败。
- 后续完成所有视觉解析容错模块后，再更新安装包产物。

## 下一步要做什么

进入模块二：字段归一化与 schema 校验。下一步会继续基于 `VisionResponseParser` 增加面向帧解析和视频汇总的归一化方法，并补充对应单测。
