# 119-视觉返回纯文本兜底落库

日期：2026-06-29

## 已完成内容

- 已读取上一轮最新快照：`118-视觉返回自动修复请求.md`。
- 已创建阶段备份：`backup/v0.1.101`。
- 已完成模块四：视觉返回纯文本兜底落库。
- `VisionResponseParser` 新增 `fallbackFrameAnalysisFromContent()` 和 `fallbackVideoSummaryFromContent()`。
- 自动修复仍失败但原始 assistant 文本非空时，帧解析会把原始文本写入 `caption`。
- 自动修复仍失败但原始 assistant 文本非空时，视频汇总会把原始文本写入 `summary`。
- 兜底文本会移除 `<think>...</think>` 并限制到 4000 字符以内，避免异常长响应污染数据库和界面。
- 已补充单测覆盖纯文本兜底、Markdown 表格兜底和空文本拒绝。
- 已更新 `docs/素材管理.md` 的视觉接口说明。
- 已新增模块溯源文档：`backup/v0.1.101/视觉返回纯文本兜底落库-前后对比.md`。

## 当前修改到哪个模块

当前完成模块：模块四，纯文本兜底落库。

至此，视频解析返回格式容错链路已完成四层策略：

1. 标准 JSON 解析。
2. 字段别名和字符串/数组归一化。
3. 一次自动格式修复请求。
4. 自动修复失败后的纯文本兜底落库。

## 具体修改的代码前后对比

### 解析器兜底结构

修改前：

```cpp
std::optional<VisionFrameAnalysis> normalizeFrameAnalysis(const QJsonObject &payload, QString *errorMessage);
std::optional<VisionVideoSummary> normalizeVideoSummary(const QJsonObject &payload, QString *errorMessage);
```

修改后：

```cpp
std::optional<VisionFrameAnalysis> fallbackFrameAnalysisFromContent(const QString &content, QString *errorMessage);
std::optional<VisionVideoSummary> fallbackVideoSummaryFromContent(const QString &content, QString *errorMessage);
```

### 帧解析失败兜底

修改前：

```cpp
if (!payload.has_value()) {
    return std::nullopt;
}
```

修改后：

```cpp
if (!payload.has_value()) {
    return fallbackFrameAnalysisFromResponse(result.body, errorMessage);
}
```

### 视频汇总失败兜底

修改前：

```cpp
if (!payload.has_value()) {
    return std::nullopt;
}
```

修改后：

```cpp
if (!payload.has_value()) {
    return fallbackVideoSummaryFromResponse(result.body, errorMessage);
}
```

### 新增测试覆盖

修改前：

```text
没有纯文本兜底结构生成测试。
```

修改后：

```text
- fallbackFrameAnalysisFromContent_preservesPlainText
- fallbackVideoSummaryFromContent_preservesMarkdownTable
- fallbackFrameAnalysisFromContent_rejectsEmptyText
```

## 验证结果

- 相关测试目标构建：通过。
  - `cmake --build build/windows-msvc-release-real --target VisionApiClientJsonTest --config Release`
- 视觉 JSON / 原始内容提取 / 字段归一化 / 纯文本兜底单测：通过，1/1。
  - `ctest --test-dir build/windows-msvc-release-real -R VisionApiClientJsonTest --output-on-failure`
- 主程序构建：通过。
  - `cmake --build build/windows-msvc-release-real --target CineVault --config Release`
- 全量单测：通过，6/6。
  - `ctest --test-dir build/windows-msvc-release-real --output-on-failure`

## 待办清单（未完成）

- 执行最终打包模块：更新构建产物和安装包，避免运行旧版本。
- 使用真实视觉接口复核 CSV、纯文本、Markdown 表格返回时的素材管理中心解析表现。

## 下一步要做什么

进入最终打包验证模块：生成新版安装包，清理临时缓存，并记录最终产物位置。
