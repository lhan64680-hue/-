# 239 - 视觉 OCR 真实回归与模块 5 发布候选收口

## 本轮目标

在快照 238 的空值落库修复基础上，完成真实项目、真实视频、真实视觉接口的端到端回归；验证结构化部分解析续跑、模块 5 降级数据契约、正式 Release 构建与干净发布候选包。

## 已完成内容

1. 只读取快照 238 作为接力入口，未回读其他历史快照。
2. 启动修复版 Windows 客户端验证可运行性。
3. 读取并验收真实解析探针日志：同一故障视频从 0 完成到 63/63，未再次出现 `video_frame_analysis.ocr_text NOT NULL` 错误。
4. 对首轮留下的 7 条部分结构化帧执行真实续跑：
   - 续跑前完整结构化帧 56/63，部分解析 7/63。
   - 续跑后完整结构化帧 60/63，部分解析 3/63。
   - 全部 63 帧仍保持解析成功，无跳过帧。
   - `ocr_text IS NULL` 数量为 0。
5. 验证剩余 3 条属于模型未返回完整结构化字段的设计内降级数据，应由模块 5 的降级警告和 `excludedPartialCount` 明示，不改成任务失败，也不进行无限重试。
6. 对当前完整工作树重新执行正式 MSVC Release 构建与 24 项全量测试。
7. 创建不含旧程序、卸载器、探针副本、数据库、帧缓存和测试日志的干净发布候选包。

## 真实端到端证据

### 首轮完整解析

视频键：

`def6b6b8-e89b-4a7e-b8b0-3185968cf141:3347`

关键日志：

```text
PROGRESS percent=54 state=1 detail=正在解析视频帧，已完成 37/63，成功 37 帧，跳过 0 帧
PROGRESS percent=55 state=1 detail=正在解析视频帧，已完成 38/63，成功 38 帧，跳过 0 帧
...
PROGRESS percent=85 state=1 detail=正在解析视频帧，已完成 63/63，成功 63 帧，跳过 0 帧
FINISHED success=1 message=视频解析完成，等待确认；仍有 7 帧结构化事实待补齐
```

原故障发生在第 38 个待处理帧附近；修复版已越过该位置并完成全部 63 帧。

### 部分结构化续跑

```text
PROGRESS percent=10 state=1 detail=继续解析视频帧，已完成 63/63，跳过 0 帧
...
FINISHED success=1 message=视频解析完成，等待确认；仍有 3 帧结构化事实待补齐
```

数据库最终只读核验：

```text
FRAME_TOTAL=63
SUCCESS=63
FACTS_COMPLETE=60
PARTIAL=3
NULL_OCR=0
TASK_STAGE=4
```

## 端到端探针入口

本轮接力时工作树中已有并继续保留以下测试入口：

- `src/app/main.cpp`：支持 `--analysis-probe-project`、`--analysis-probe-video-key`、`--analysis-probe-log` 参数，并在任务完成后返回明确退出码。
- `src/app/AppContext.h/.cpp`：提供 `startAnalysisProbe()`、进度信号与完成信号，将项目打开、全局目录同步、素材入队和解析完成串成真实端到端链路。

正常无参数启动仍进入原有 GUI，不受探针入口影响。

## Windows UI 自动化说明

`computer-use` 能发现并启动候选客户端，但当前 Windows 图形捕获接口连续返回：

```text
SetIsBorderRequired failed: 不支持此接口 (0x80004002)
```

按照技能约束，观察失败后未继续盲目点击界面。模块 5 UI 继续由以下证据覆盖：

- QML Release 编译和 QML cache 构建通过。
- `MaterialCenterUiContractTest` 通过。
- `MaterialCenterQueryServiceTest` 通过。
- 真实数据库已形成 `excludedPartialCount=3` 对应的数据场景。
- 最终可见交互仍需用户在候选包中人工确认。

## 构建与测试结果

- `CINEVAULT_ENABLE_FFMPEG=ON`
- `CINEVAULT_ENABLE_LOCAL_SEARCH=ON`
- MSVC Release 构建：通过，无待构建增量。
- CTest：24/24 通过，0 失败。
- `git diff --check`：通过。
- 无残留 CineVault、CineVaultProbe、CMake、CTest 或 Ninja 进程。

## 最终发布候选包

目录：

`G:\data\app\DIT-tools\dit-tools-src\cinevault-pro\build\test-packages\CineVault-module5-rc-20260714`

程序：

`G:\data\app\DIT-tools\dit-tools-src\cinevault-pro\build\test-packages\CineVault-module5-rc-20260714\CineVault.exe`

SHA-256：

`B2485C356F6F5944F0BDDE18571930E2761E1412B34CF1AB51C2101A2BBE4EEA`

包核验：

- 文件数：577。
- 大小：约 0.567 GiB。
- 正式 `CineVault.exe` 数量：1。
- 与 MSVC Release 构建产物哈希一致。
- 包含 Qt、SQLite、FFmpeg 动态库、FFmpeg/FFprobe CLI、ONNX Runtime、BGE 模型和 VC Runtime 安装程序。
- 不含素材中心数据库、探针日志、帧缓存、旧版程序和卸载器。

## 缓存与工作树

- Release 构建目录约 0.144 GiB。
- 所有测试包合计约 1.322 GiB。
- 可复用构建缓存约 0.489 GiB。
- 总量远低于 20 GiB，不删除仍有真实回归证据的旧测试包。
- 模块 1、2、3、4、5、OCR 修复和端到端探针相关修改均仍未提交，必须继续保留。
- 与任务无关的原有未跟踪 ZIP 未读取、未修改、未移动、未删除。
- 固定帧间隔视觉采样约束保持不变；未加入变化帧、场景帧、关键帧、自适应采样或 ASR。

## 当前结论与下一步

模块 1 至模块 5 的开发、自动化测试、真实视觉解析回归和发布候选构建均已完成。当前没有未完成的代码开发项。

下一步只剩发布门禁：用户人工确认候选包的模块 5 可见交互后，明确决定是否修改正式版本号、制作安装包并对外发布。未经确认不得发布。

