# 108-立即更新安装脚本修复

日期：2026-06-27

## 已完成内容

- 已继续修复“立即更新”后应用退出但没有继续安装的问题。
- 已把退出后执行的 PowerShell 更新脚本迁移到系统临时目录，避免继续依赖应用缓存目录下的复杂路径。
- 已为更新脚本补充显式提权、执行日志和异常捕获，避免脚本失败时静默无反馈。
- 已保留现有“下载完成后弹出统一确认框”的交互，不改下载与待安装状态逻辑。

## 当前修改到哪个模块

当前完成到新增子模块：立即更新安装脚本修复与加固。

修改文件：
- `dit-tools-src/cinevault-pro/src/application/UpdateService.cpp`
- `backup/v0.1.90/模块7-立即更新安装脚本修复对比.md`

## 具体修改的代码前后对比

### 更新脚本改为放在系统临时目录

修改前：

```cpp
QString installerScriptPath()
{
    return QDir(Paths::updatesRoot()).filePath(QStringLiteral("apply-update.ps1"));
}
```

修改后：

```cpp
QString installerScriptPath()
{
    return QDir(QDir::tempPath()).filePath(QStringLiteral("CineVaultUpdater/apply-update.ps1"));
}
```

### 更新脚本从静默失败改为可追踪失败

修改前：

```cpp
scriptLines << QStringLiteral("$ErrorActionPreference = 'SilentlyContinue'")
            << QStringLiteral("$process = Start-Process -FilePath $installerPath -ArgumentList $installerArgs -Wait -PassThru")
            << QStringLiteral("if (Test-Path $appExe) { Start-Process -FilePath $appExe | Out-Null }");
```

修改后：

```cpp
scriptLines << QStringLiteral("$ErrorActionPreference = 'Stop'")
            << QStringLiteral("$logPath = %1").arg(toPowerShellLiteral(nativeLogPath))
            << QStringLiteral("try {")
            << QStringLiteral("    $process = Start-Process -FilePath $installerPath -ArgumentList $installerArgs -Verb RunAs -Wait -PassThru")
            << QStringLiteral("    if ($process.ExitCode -eq 0 -and Test-Path $appExe) {")
            << QStringLiteral("        Start-Process -FilePath $appExe | Out-Null")
            << QStringLiteral("    }")
            << QStringLiteral("} catch {")
            << QStringLiteral("    Write-UpdateLog ('更新脚本执行失败：' + $_.Exception.Message)")
            << QStringLiteral("}");
```

## 验证结果

- 带 Visual Studio 构建环境的增量编译已通过。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure` 已通过。
- `%TEMP%\\CineVaultUpdater` 下的隐藏 PowerShell 冒烟测试已成功写出日志，确认新的脚本目录可稳定执行。

## 待办清单（未完成）

- 创建或更新 `v0.1.88` GitHub Release 资产。
- 用已安装旧版本对最新 Release 做一次真实“自动检查 -> 自动下载 -> 立即更新 -> 自动重启”闭环验证。
- 补 `106-自动更新构建验证.md` 最终版，并同步发布说明。

## 下一步要做什么

继续模块 5 收口：
- 发布用于联调的最新 GitHub Release。
- 用真实安装版本验证“立即更新”闭环是否完全恢复。
- 如闭环通过，再提交并推送这次脚本修复。
