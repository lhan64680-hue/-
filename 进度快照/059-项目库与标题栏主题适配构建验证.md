# 进度快照 059

- 时间：2026-06-26 16:34:20 +08:00
- 已完成内容：完成项目库与标题栏主题适配全流程开发、构建验证、烟测和临时缓存清理。
- 当前修改模块：项目库与标题栏主题适配整体验证模块。
- 阶段备份：已创建 `backup/v0.1.48`，并更新 `backup/v0.1.48/项目库与标题栏主题适配-前后对比.md` 记录本阶段关键代码前后对比和验证结果。
- 代码前后对比：
  - 修改前：没有「项目库」标签和项目卡片入口，项目只能通过按钮新建/打开；项目列表只保留 recentProjects 最近 10 条。
  - 修改后：新增长期项目库索引 `knownProjects`、项目卡片列表、项目库工作区；标签顺序为「项目库」「素材库」「素材管理中心」「报表」「任务」，点击项目卡片会打开项目并进入素材库。
  - 修改前：未打开项目时进入素材库会显示空素材列表，项目库场景下仍复用素材搜索逻辑。
  - 修改后：未打开项目进入素材库显示引导态；项目库页隐藏素材源栏、检查器和底部任务栏；全局搜索在项目库中搜索项目。
  - 修改前：Windows 原生标题栏不跟随应用暗黑主题，可能保持白色。
  - 修改后：新增 `WindowThemeController`，通过 DWM 同步标题栏深浅、背景色、文字色和边框色。
- 验证结果：
  - 构建命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`
  - 最终产物：`output/v0.1.57/CineVault-Setup-v0.1.57.exe`
  - 安装包大小：`55273037`
  - SHA256：`68CD06F970F4DB14E3C4287AC9651A9035F31B9A74DB7442AF764C6650E66D9B`
  - `ctest`：命令成功退出，当前构建目录输出 `No tests were found!!!`
  - 5 秒启动烟测：通过，状态 `SMOKE_RUNNING_AFTER_5S`，`NO_QML_LOG`
  - 空白检查：`git diff --check` 未发现空白错误，仅有既有 LF/CRLF 提示。
  - 临时缓存：`output/_smoke_runtime` 未残留；空的 `output/_installer_staging` 已删除。
- 待办清单（未完成）：
  - 安装或运行 `output/v0.1.57/CineVault-Setup-v0.1.57.exe` 后人工复核项目库卡片、新建/打开项目流程、缺失项目移除入口。
  - 在暗黑/明亮/跟随系统三种主题下人工确认 Windows 标题栏颜色同步。
- 下一步要做什么：人工复核实际 UI；如果项目库卡片信息需要更多统计项，再在项目库列表模型中补充素材源数量或素材数量。

