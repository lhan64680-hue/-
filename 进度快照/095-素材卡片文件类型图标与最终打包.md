# 095-素材卡片文件类型图标与最终打包

日期：2026-06-27

## 已完成内容

- 已继续上一模块快照：`094-常见图片格式识别增强.md`。
- 已在模块二开始前执行源码备份，生成 `backup/v0.1.79`。
- 已完成模块二：素材卡片文件类型图标展示。
- 当缩略图不可用时，`AssetCard` 现在按素材类型显示兜底图标：
  - 视频：`VID`
  - 音频：`AUD`
  - 图片：`IMG`
  - 字幕：`SUB`
  - 工程：`PRJ`
  - 文档：`DOC`
  - 压缩包：`ZIP`
  - 其他：`FILE`
- 兜底图标增加了文件形态、顶部色条、类型缩写、扩展名角标和类型专属颜色。
- 类型标签 `StatusChip` 改为跟随素材类型颜色。
- 已执行最终真实工作流 Release 打包，更新安装包到 `output/v0.1.73/CineVault-Setup-v0.1.73.exe`。
- 已新增模块溯源文档：`backup/v0.1.79/素材卡片文件类型图标-前后对比.md`。

## 当前修改到哪个模块

当前模块：素材卡片 UI 兜底展示。

修改文件：

- `dit-tools-src/cinevault-pro/src/ui/qml/components/AssetCard.qml`

本轮完整修改还包含上一模块：

- `dit-tools-src/cinevault-pro/src/core/scan/FileTypeService.cpp`

## 具体修改的代码前后对比

### 1. AssetCard 新增类型图标映射

修改前：

```qml
Rectangle {
    property string title: ""
    property string tag: ""
    property string thumbnailPath: ""
}
```

修改后：

```qml
Rectangle {
    id: assetCard

    property string title: ""
    property string tag: ""
    property string thumbnailPath: ""
    readonly property string extensionLabel: {
        var dot = title.lastIndexOf(".")
        var raw = dot >= 0 ? title.substring(dot + 1) : tag
        raw = raw.length > 0 ? raw.toUpperCase() : "FILE"
        return raw.substring(0, 5)
    }

    function fileIconKind() {
        if (tag.indexOf("视频") >= 0) return "VID"
        if (tag.indexOf("音频") >= 0) return "AUD"
        if (tag.indexOf("图片") >= 0) return "IMG"
        if (tag.indexOf("字幕") >= 0) return "SUB"
        if (tag.indexOf("工程") >= 0) return "PRJ"
        if (tag.indexOf("文档") >= 0) return "DOC"
        if (tag.indexOf("压缩包") >= 0) return "ZIP"
        return "FILE"
    }
}
```

### 2. 缩略图占位改为文件类型图标

修改前：

```qml
Rectangle {
    anchors.fill: parent
    anchors.margins: 10
    radius: 12
    visible: previewImage.status !== Image.Ready
    gradient: Gradient {
        GradientStop { position: 0.0; color: Theme.isDark ? "#35589E" : "#DCE6F8" }
        GradientStop { position: 1.0; color: Theme.isDark ? "#11151D" : "#F7FAFF" }
    }
}
```

修改后：

```qml
Rectangle {
    id: fallbackPreview
    anchors.fill: parent
    visible: previewImage.status !== Image.Ready
    radius: 18
    gradient: Gradient {
        GradientStop { position: 0.0; color: Qt.rgba(fileIconTint().r, fileIconTint().g, fileIconTint().b, Theme.isDark ? 0.72 : 0.28) }
        GradientStop { position: 1.0; color: Theme.isDark ? "#11151D" : "#F7FAFF" }
    }
}
```

### 3. 类型标签颜色随类型变化

修改前：

```qml
StatusChip {
    label: tag
    tint: Theme.green
}
```

修改后：

```qml
StatusChip {
    label: tag
    tint: fileIconAccent()
}
```

## 验证结果

- 模块一真实工作流 Release 增量构建通过。
- 模块一真实工作流 `ctest` 通过：3/3。
- 模块一启动烟测通过。
- 模块二真实工作流 Release 增量构建通过。
- 模块二真实工作流 `ctest` 通过：3/3。
- 模块二启动烟测通过。
- 最终真实工作流 Release 安装包已生成：`output/v0.1.73/CineVault-Setup-v0.1.73.exe`。
- 安装包大小：55,526,451 bytes。
- SHA256：`B221088A8237D23DEFFB5E46452A252AFF0E5E3032E03F177D0727D9D0A52670`。
- 打包 staging 检查：`STAGING_RECURSIVE_ITEM_COUNT=0`。
- 当前无残留 `CineVault` 进程。

## 待办清单（未完成）

- 人工在素材库查看音频、文档、工程文件、压缩包、字幕和其他文件，确认卡片显示类型图标与扩展名角标。
- 人工查看已有图片/视频素材，确认真实缩略图加载成功时不会被兜底图标遮挡。
- 人工用 `webp/tiff/psd/heic/avif` 等样本重新导入，确认进入图片分类并触发缩略图生成。

## 下一步要做什么

安装或运行 `output/v0.1.73/CineVault-Setup-v0.1.73.exe`，在素材库用混合素材目录复核：

1. 常见图片格式是否归类为图片并生成缩略图。
2. 无缩略图的非图片文件是否显示类型图标。
3. 素材卡片在大图卡片和不同缩略图尺寸下是否无文字重叠。
