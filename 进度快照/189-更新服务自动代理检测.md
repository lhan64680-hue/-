# 进度快照 189

- 时间：2026-07-02 12:06:15
- 当前模块：更新服务自动代理检测与下载策略

## 已完成内容

- 已读取上一模块快照：`进度快照/188-设置页更新下载渠道与版本号显示.md`。
- 已完成阶段备份：
  - `backup/v0.1.147`
  - `backup/v0.1.147/更新服务自动代理检测代码对比.md`
- 已调整 `UpdateService` 代理策略：
  - `0` 自动检测代理。
  - `1` 手动代理。
  - `2` 直连下载。
- 自动检测代理现在覆盖：
  - Qt 系统代理查询结果。
  - 环境变量：`HTTPS_PROXY`、`https_proxy`、`HTTP_PROXY`、`http_proxy`、`ALL_PROXY`、`all_proxy`。
  - 常见本机代理地址和端口：如 `127.0.0.1:7890`、`localhost:7890`、`10809`、`1080` 等，并会枚举本机 IPv4 地址。
- 手动代理支持无协议输入：
  - `127.0.0.1:7890` 会规范化为 `http://127.0.0.1:7890`。
- 检查更新和下载更新都改为统一读取配置后的代理地址。
- 状态文案已区分：
  - 自动检测代理。
  - 手动代理。
  - 直连下载。
  - 代理失败后直连兜底。
- 已补充 `UpdateServiceTest`：
  - 代理地址规范化。
  - 环境变量代理读取。
  - 常见本机代理候选端口。

## 当前修改到哪个模块

- 更新服务网络下载模块。

## 具体修改的代码前后对比

修改前：只使用系统代理查询结果。

```cpp
const auto proxyUrl = systemProxyUrl();
setStatusMessage(proxyUrl.isEmpty()
    ? QStringLiteral("正在检查最新发布版本...")
    : QStringLiteral("正在通过系统代理检查最新发布版本..."));
launchCheckProcess(proxyUrl, !proxyUrl.isEmpty());
```

修改后：按设置项统一选择代理。

```cpp
QString proxyErrorMessage;
const auto proxyUrl = configuredProxyUrl(&proxyErrorMessage);
if (!proxyErrorMessage.isEmpty()) {
    setBusy(false);
    setStatusMessage(proxyErrorMessage);
    return;
}

launchCheckProcess(proxyUrl, !proxyUrl.isEmpty());
```

自动检测候选来源：

```cpp
appendUnique(&candidates, systemProxyUrl());
for (const auto &proxyUrl : proxyUrlsForEnvironment(QProcessEnvironment::systemEnvironment())) {
    appendUnique(&candidates, proxyUrl);
}
for (const auto &proxyUrl : localProxyCandidates()) {
    appendUnique(&candidates, proxyUrl);
}
```

## 验证结果

- `cmake --build --preset windows-msvc-release-real --target UpdateServiceTest`：通过。
- `ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real -R UpdateServiceTest --output-on-failure`：通过。
- `cmake --build --preset windows-msvc-release-real`：通过。

## 待办清单（未完成）

- 运行完整测试套件。
- 重新生成安装包，确保发布构建产物包含设置页和更新逻辑。
- 清理构建临时缓存。
- 如需对外分发，后续发布新版本到 GitHub Release。

## 下一步要做什么

- 进入第 3 模块：完整测试、构建安装包、清理临时缓存。

