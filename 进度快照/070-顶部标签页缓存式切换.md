# 进度快照 070

- 时间：2026-06-27
- 已完成内容：顶部工作区切换从单个 `Loader` 销毁重建改为多个访问后常驻的 `Loader`，减少切换标签时重新创建 QML 页面和重复触发页面初始化的卡顿。
- 当前修改模块：模块 1，顶部标签页卡顿修复。
- 阶段备份：
  - `backup/v0.1.55`
- 修改文件：
  - `dit-tools-src/cinevault-pro/src/ui/qml/Main.qml`
  - `docs/素材库与素材管理体验优化方案.md`

## 代码前后对比

### 工作区加载方式

修改前：

```qml
Loader {
    Layout.fillWidth: true
    Layout.fillHeight: true
    sourceComponent: {
        if (root.shellViewModel.currentWorkspace === root.shellViewModel.projectLibraryWorkspaceId) {
            return projectLibraryWorkspaceComponent
        }
        ...
        return libraryWorkspaceComponent
    }
}
```

修改后：

```qml
Item {
    Layout.fillWidth: true
    Layout.fillHeight: true

    Loader {
        anchors.fill: parent
        active: root.projectLibraryWorkspaceLoaded
        visible: root.shellViewModel.currentWorkspace === root.shellViewModel.projectLibraryWorkspaceId
        asynchronous: true
        sourceComponent: projectLibraryWorkspaceComponent
    }

    Loader {
        anchors.fill: parent
        active: root.materialCenterWorkspaceLoaded
        visible: root.shellViewModel.currentWorkspace === root.shellViewModel.materialCenterWorkspaceId
        asynchronous: true
        sourceComponent: materialCenterWorkspaceComponent
    }
}
```

### 工作区访问状态

修改前：无工作区访问缓存状态，每次 `sourceComponent` 变化都可能销毁当前工作区。

修改后：

```qml
property bool projectLibraryWorkspaceLoaded: true
property bool libraryWorkspaceLoaded: false
property bool materialCenterWorkspaceLoaded: false
property bool reportWorkspaceLoaded: false
property bool jobsWorkspaceLoaded: false

function rememberWorkspace(workspace) {
    ...
}
```

## 验证结果

- 首次直接执行 `cmake --build --preset windows-msvc-release --config Release` 失败，原因是未进入 MSVC 编译环境，编译器找不到 `type_traits`。
- 通过项目工具链封装执行：

```powershell
. .\tool\windows_toolchain.ps1
$ctx = Get-CineVaultBuildContext
Push-Location .\dit-tools-src\cinevault-pro
Invoke-VcVarsCommand 'cmake --build --preset windows-msvc-release --config Release'
Pop-Location
```

- 结果：最小 GUI 构建成功，`CineVault.exe` 链接完成。

## 待办清单（未完成）

- 模块 2：素材库方向键选择与空格全屏播放。
- 模块 3：素材库缩略图尺寸调节。
- 模块 4：素材管理中心方向键选择、详细摘要和逐帧搜索。
- 模块 5：源素材栏改名与折叠展开。
- 模块 6：设置弹窗背景模糊。
- 模块 7：全局验证、打包与缓存清理。

## 下一步要做什么

执行模块 2 前创建下一阶段备份，然后补齐真实和 Minimal 素材库 ViewModel 的索引选择接口，并在 `LibraryWorkspace.qml` 接入方向键与空格全屏播放。
