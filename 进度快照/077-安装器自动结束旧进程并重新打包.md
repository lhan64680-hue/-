# 进度快照 077

- 时间：2026-06-27
- 已完成内容：安装器新增自动结束旧版 `CineVault.exe` 进程规则，并重新生成安装包，避免安装时旧进程占用导致无法覆盖。
- 当前修改模块：模块 8，安装器旧进程自动结束与重新打包。
- 阶段备份：
  - `backup/v0.1.62`
- 修改文件：
  - `installer/windows/cinevault.iss`
  - `docs/素材库与素材管理体验优化方案.md`

## 代码前后对比

### 安装器默认关闭应用规则

修改前：

```iss
SetupLogging=yes
PrivilegesRequired=admin
```

修改后：

```iss
SetupLogging=yes
PrivilegesRequired=admin
CloseApplications=no
RestartApplications=no
```

说明：不再依赖 Inno/Restart Manager 的默认“关闭应用”提示，避免提示出现但无法真正关闭旧进程。

### 自动结束旧进程

修改前：安装脚本没有 `[Code]` 段，安装和卸载前不会主动强制结束 `CineVault.exe`。

修改后：

```iss
function StopCineVaultProcesses(const Phase: String): Boolean;
var
  Attempt: Integer;
  ResultCode: Integer;
begin
  Result := True;

  if not IsCineVaultRunning() then
    exit;

  Log('CineVault process detected during ' + Phase + '; forcing shutdown before installation continues.');

  for Attempt := 1 to 3 do
  begin
    Exec(ExpandConstant('{sys}\taskkill.exe'), '/F /T /IM CineVault.exe',
      '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
    Sleep(500);

    if not IsCineVaultRunning() then
      exit;
  end;

  MsgBox('检测到旧版影资管家仍在运行，安装程序无法自动结束进程。请手动退出影资管家后重新运行安装包。',
    mbError, MB_OK);
  Result := False;
end;
```

触发点：

```iss
function InitializeSetup(): Boolean;
function PrepareToInstall(var NeedsRestart: Boolean): String;
function InitializeUninstall(): Boolean;
```

说明：安装初始化、正式写文件前、卸载初始化都会检查并强制结束旧版 `CineVault.exe`，最多重试 3 次；若仍失败则取消安装或卸载。

## 验证结果

- 阶段备份：`bash tool/create_backup.sh`，生成 `backup/v0.1.62`。
- 重新打包命令：`powershell -ExecutionPolicy Bypass -File tool/build_windows.ps1 -RealWorkflow`。
- 构建结果：C++ 构建为最新状态，Inno Setup `[Code]` 段编译成功。
- 新安装包：`output/v0.1.63/CineVault-Setup-v0.1.63.exe`。
- 安装包大小：`55439436` 字节。
- 安装包 SHA256：`1604BD8670896B52354439B3DABFE5A3137FCD72E57F9856AA2F15DA7E9FA33B`。
- 测试命令：`ctest --test-dir dit-tools-src/cinevault-pro/build/windows-msvc-release-real --output-on-failure`。
- 测试结果：命令成功执行，当前构建目录提示 `No tests were found!!!`。
- 临时目录：`output/_installer_staging` 无残留版本目录。

## 待办清单（未完成）

- 运行新版安装包并人工复核：旧版 `CineVault.exe` 正在运行时，安装器是否能自动结束旧进程并完成覆盖安装。
- 覆盖安装后启动应用，复核上一轮素材库、素材管理中心和设置弹窗功能是否正常。

## 下一步要做什么

下一位开发者或智能体接力时，先读取本快照和 `docs/素材库与素材管理体验优化方案.md`。如人工安装验证仍发现进程无法结束，优先查看安装日志中的 `CineVault process detected` 记录，并进一步补充更严格的进程路径匹配或延长等待时间。
