# 进度快照 058

- 时间：2026-06-26 16:28:32 +08:00
- 已完成内容：新增 Windows 原生窗口标题栏主题适配能力，暗黑/明亮/跟随系统主题变化时会同步设置标题栏深浅、背景色、文字色和边框色。
- 当前修改模块：标题栏主题适配模块，涉及 `WindowThemeController.h/.cpp`、`AppContext.h/.cpp`、`Main.qml`、`CMakeLists.txt`。
- 阶段备份：沿用本阶段开始前创建的 `backup/v0.1.48`。
- 代码前后对比：
  - 修改前：QML 内容区会随 `Theme` 切换，但 Windows 原生窗口标题栏没有主题同步入口，暗黑模式下可能仍显示白色标题栏。
  - 修改后：新增 `WindowThemeController`，Windows 下通过 DWM 设置 `DWMWA_USE_IMMERSIVE_DARK_MODE`、`DWMWA_CAPTION_COLOR`、`DWMWA_TEXT_COLOR`、`DWMWA_BORDER_COLOR`，非 Windows 平台为空实现。
  - 修改前：QML 根窗口没有在主题变化时通知原生标题栏。
  - 修改后：`Main.qml` 在窗口创建、可见性变化、`Theme.isDark/topBar/text` 变化时重新应用标题栏主题。
- 待办清单（未完成）：
  - 运行真实流程构建，修复可能的编译/QML 问题。
  - 补充本阶段前后对比文档最终内容。
  - 运行 `ctest`、5 秒烟测，清理临时缓存并生成最终快照。
- 下一步要做什么：执行构建验证与烟测。

